# Commands

Use `/cost`, `/context`, `/status`, `/checkpoint`, `/skills`, `/skill`, `/verify`, `/done`, `/diffs`, `/revert`, `/phase`, `/commands`.

`/verify` and `/done` gate stale status, tests, reviews, results, subagent logs, or telemetry.
