You are reviewing the PRE-FLIGHT design of R-tier R3 — iter-3.
iter-1 returned REJECT (4 fixes), iter-2 returned REJECT (3 new findings).
This iter-3 applies all 3 iter-2 findings. Your job: confirm closure.

CONTEXT
- Test file: compact_v5/MAIN/agent/tests/r_tier/test_r3_three_subagents.py (now ~410 lines after fixes)
- Cost cap: $0.50, Model: claude-haiku-4-5 AU.

ITER-2 FINDINGS + ITER-3 FIXES

Finding #1 (AXIS A medium): read_file evidence collected across parent + child sessions —
parent could discover read_file via tool_search and read all 3 files itself.
FIX APPLIED:
  - Capture `parent_session_id = agent._engine.session_id` BEFORE run() (line ~232).
  - Split read_file dispatches into `parent_read_file_dispatches` (session_id == parent's)
    and `child_read_file_dispatches` (session_id != parent's).
  - `files_read_by_children` set is built ONLY from child dispatches.
  - Added explicit assertion that NO parent-session read_file dispatch targets any of
    the 3 utility files (parent_target_hits must be empty).

Finding #2 (AXIS A medium): files_read assertion didn't tie each child to its assigned file.
FIX APPLIED:
  - For each `task` dispatch, scan parameters.prompt for exactly ONE of the 3 file names.
  - Build `task_prompt_file_targets` list (3 entries, one per dispatch, each a list of hits).
  - Assert: 3 singletons (each prompt mentions exactly ONE file).
  - Assert: targeted_files set covers all 3 files (one-to-one task→file mapping).

Finding #3 (AXIS C reject): G3 coverage was claimed but not falsified.
FIX APPLIED:
  - Updated docstring to scope test to **Phase 9 only**. coordinator_mode_enabled=True
    is kept (so the augmented system prompt still exercises the path) but Block G3
    is no longer claimed.
  - Block G3's coverage stays in tests/integration/test_block_g3.py unit tests.

=== INLINED CHANGED CODE (iter-3 deltas) ===

# Updated docstring (lines 1-43):
"""R-tier R3 — three sub-agent dispatches + parent synthesis.

Per PS_V5_TEST_PLAYBOOK / PS_V5_TEST_SET §Tier 1 R3:
  Validates Phase 9 task tool (sub-agent spawn) end-to-end on real
  Bedrock:
    ...

Codex iter-2 fix #3 — scope: this test validates **Phase 9 only**.
  ...R3 keeps coordinator_mode_enabled=True only to exercise the
  augmented system prompt without claiming it.

Validation pillars:
  - subagent_dispatches audit entries == 3 (Phase 9 task tool spawn).
  - All 3 dispatches are subagent_type='explore'.
  - Each task dispatch prompt names exactly ONE assigned file.
  - The 3 utility files were read by CHILD sessions (not parent).
  - Zero parent-session read_file dispatches against utils_*.py.
  - findings.md exists and mentions all 3 function names + TODOs.
  ...
"""

# parent_session_id capture (after agent build, before run):
        agent._engine._discovered_tool_names = {"task", "write_file"}
        # Codex iter-2 fix #1 (medium): capture the parent's session_id
        # BEFORE run(), so we can split audit entries into parent vs
        # child sessions for the read_file evidence assertion.
        parent_session_id = agent._engine.session_id
        t0 = time.time()
        result = agent.run(_R3_PROMPT, output_fn=_stdout_capture)

# Session-aware read_file extraction:
        all_read_file_dispatches = [
            e for e in audit_entries
            if e.get("tool_name") == "read_file" and e.get("action") == "tool_dispatch"
        ]
        parent_read_file_dispatches = [
            e for e in all_read_file_dispatches
            if e.get("session_id") == parent_session_id
        ]
        child_read_file_dispatches = [
            e for e in all_read_file_dispatches
            if e.get("session_id") != parent_session_id
        ]
        files_read_by_children = set()
        for e in child_read_file_dispatches:
            params = e.get("parameters") or {}
            fp = str(params.get("file_path") or params.get("path") or "")
            for fname in ("utils_a.py", "utils_b.py", "utils_c.py"):
                if fname in fp:
                    files_read_by_children.add(fname)

# task_prompt_file_targets (one-to-one mapping):
        task_prompt_file_targets: list = []
        for d in task_dispatches:
            params = d.get("parameters") or {}
            prompt_text = str(params.get("prompt") or "")
            hit_files = [
                fname for fname in ("utils_a.py", "utils_b.py", "utils_c.py")
                if fname in prompt_text
            ]
            task_prompt_file_targets.append(hit_files)

# All assertions:
        assert result.stop_reason != "max_turns"
        assert len(task_dispatches) == 3
        assert len(explore_dispatches) == 3
        # Children-read (parent must NOT bypass children):
        assert files_read_by_children == {"utils_a.py", "utils_b.py", "utils_c.py"}, ...
        parent_target_hits = []
        for e in parent_read_file_dispatches:
            params = e.get("parameters") or {}
            fp = str(params.get("file_path") or params.get("path") or "")
            for fname in ("utils_a.py", "utils_b.py", "utils_c.py"):
                if fname in fp:
                    parent_target_hits.append((fname, fp))
        assert not parent_target_hits, ...
        # One-to-one task→file mapping:
        targets_singletons = [t for t in task_prompt_file_targets if len(t) == 1]
        assert len(targets_singletons) == 3, ...
        targeted_files = {t[0] for t in targets_singletons}
        assert targeted_files == {"utils_a.py", "utils_b.py", "utils_c.py"}, ...
        # Findings content:
        assert findings_fp.is_file()
        assert len(function_hits) == 3
        assert len(marker_hits) == 3
        assert cost_used <= _R3_COST_CAP_USD

=== END INLINED CHANGES ===

WORKER SELF-CONFIRMATION:
- pytest --collect-only: SKIPPED (RUN_REAL_BEDROCK not set), test discovers cleanly.
- session_id is set in QueryEngine.__init__ at query_engine.py:241-244 if None;
  AUDIT.log uses self.session_id (query_engine.py:431, 562, 704, 748, 784, 989, 1019)
  for ALL audit entries from the parent.
- Sub-agents call spawn_subagent → _new_child_engine → fresh QueryEngine →
  fresh session_id (spawn.py:77 docstring confirms).
- AUDIT entries from a child carry the child's session_id, NOT the parent's.
- Therefore session_id != parent_session_id reliably distinguishes child entries.

PRODUCE THREE INDEPENDENT VERDICTS.

=== AXIS A — Test design soundness ===
- iter-2 finding #1 closed by parent-vs-child session split + parent_target_hits assertion?
- iter-2 finding #2 closed by task_prompt_file_targets singleton + coverage assertions?
- iter-2 finding #3 closed by docstring scoping to Phase 9 only?
- Are there any new issues introduced by the fixes?
- What concrete v5 regression would these new assertions catch?

Output:
AXIS A VERDICT: APPROVE | APPROVE_WITH_FIXES | REJECT
Findings: [severity] file:line — issue — fix

=== AXIS B — Cost-control soundness ===
- Same cost estimate as iter-2 (~$0.19) since the new assertions are post-run.
- Re-confirm the IterationBudget(24) cap and == 3 dispatch limit prevent runaway spend.

Output:
AXIS B VERDICT: APPROVE | APPROVE_WITH_FIXES | REJECT
Findings:

=== AXIS C — Coverage of v5 EMPIRICAL evidence ===
- Phase 9 coverage: clearer now — task tool dispatch, agent_type='explore' routing,
  child session isolation (fresh session_id per spawn), audit logging.
- One-to-one task→file mapping is a strong falsifier for "did the parent actually
  scope its dispatches?"
- Children's read_file evidence forces the children to do the work.
- v5-only: NO request for v4 side-by-side runs.

Output:
AXIS C VERDICT: APPROVE | APPROVE_WITH_FIXES | REJECT
Coverage gap (if any):

=== FINAL ===
PRE-FLIGHT VERDICT: APPROVE_FOR_AWS_CALL | REJECT
