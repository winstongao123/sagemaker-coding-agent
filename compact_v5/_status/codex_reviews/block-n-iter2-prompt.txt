Block N ITER 2. Codex iter-1 = REJECT with 2 findings, both fixed:

1. HIGH: notebook_edit uses notebook_path, not file_path. Fix: NEW
   _extract_target_path helper mapping tool name → actual path arg.
2. MEDIUM: path conflicts not canonicalized. Fix: os.path.normpath(
   os.path.abspath(raw)) before grouping.

Diff: <prev>..HEAD (iter-2 commit 136f41b).

Modified files:
- core/parallel_dispatch.py: _extract_target_path + canonicalization.
- tests/integration/test_block_n.py: 2 new lock tests + updated
  existing test for canonicalized assertions.

Tests: 745 pass + 14 skip (was 743; +2).

Verify both fixes lock cleanly. APPROVE if no new findings.
