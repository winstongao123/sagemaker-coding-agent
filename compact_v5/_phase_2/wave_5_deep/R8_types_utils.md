# R8 — Runnable types/constants/utils slice (exhaustive line-by-line scan)

**Scanned**: 2026-05-01
**Source root**: `D:/Github/sagemaker-coding-agent/_archive/compare_code/gg-claude-code-runnable/src/`
**Slice covered**: `constants/` (22 files), `schemas/` (1 file), `types/` (8 ts + 4 generated proto), `utils/` (top-level: 250+ files plus 30 subdirs — read all small/medium top-level files; skimmed large platform files), `state/` (6 files), `migrations/` (11 files), `coreTypes.generated.ts`, `global.d.ts`, `projectOnboardingState.ts`.
**Compared against**:
- v4 baseline: `compact_v4/MAIN/agent/sagemaker_agent.py` (12,088 lines, monolithic Python).
- Existing plan: `compact_v5/_phase_2/synthesis/V5_PHASE_2_PLAN_v3.md` (Blocks 0-T).
- Q1 matrix: `compact_v5/_phase_2/wave_4/Q1_EVIDENCE_MATRIX.md` (172 rows).
- Wave 5 sibling: `compact_v5/_phase_2/wave_5/RUNNABLE_NEW_FUNCTIONALITY.md` (0 new findings — but that scan focused on services/, not utils/).

**Constraint set v5.0.1**: single-user SageMaker, Bedrock-only, v4 chat.ipynb canonical UI, NO DEFERRALS. So OUT-OF-SCOPE filters: claude.ai OAuth, multi-user/teammate, ink/React, plugins/MCP, IDE bridge, voice/computer-use, GitHub-app, growthbook/statsig analytics, attestation/billing-header, fast-mode billing, hooks-as-config (HookCommandSchema is plugin/settings-driven, not v4 callbacks).

---

## NEW findings (utility patterns potentially useful for v5, not in v5.0.1 plan)

| # | Pattern | Runnable file:line | v4 has? | v5.0.1 needs? | Target Block | How to graft on v4 (Python equivalent) | Status |
|---|---|---|---|---|---|---|---|
| 1 | `lazySchema(factory)` — defer Zod schema construction to first access. Returns memoized factory. | `utils/lazySchema.ts:1-9` | No (v4 has no schema layer; tool inputs are Python dicts validated ad-hoc) | Optional | Block T (tools) | Trivial Python equivalent: `functools.cache` on a builder. Useful only if v5 introduces pydantic/jsonschema for tool inputs (Q3 better-than axis). Otherwise drop. | OPTIONAL |
| 2 | `semanticBoolean()` / `semanticNumber()` — coerce model-emitted `"false"`/`"30"` JSON strings to bool/number, while rejecting truly invalid input (unlike `z.coerce`). | `utils/semanticBoolean.ts:1-30`, `utils/semanticNumber.ts:1-37` | Partial — v4 trusts Bedrock tool_use input shapes; bugs are reactive | **Yes** | Block T (tool input validation) | Wrap each tool's argument access with `_coerce_bool(v)`/`_coerce_int(v)` helpers in sagemaker_agent.py. Documented bug class: model occasionally quotes booleans/numbers in tool_use input. Cheap insurance (~30 LOC). | RECOMMEND-ADD |
| 3 | `withResolvers<T>()` — Promise.withResolvers polyfill. Returns `{promise, resolve, reject}`. | `utils/withResolvers.ts:1-13` | N/A (Python has `asyncio.Future`) | No (Python has native equivalent) | N/A | Skip — `loop.create_future()` already provides this. | DROP |
| 4 | `objectGroupBy(items, keySelector)` — TC39 Object.groupBy polyfill (Object.create(null) result, no prototype pollution). | `utils/objectGroupBy.ts:1-18` | N/A | No (Python `itertools.groupby` + `collections.defaultdict`) | N/A | Skip. | DROP |
| 5 | `intersperse / count / uniq` array helpers. | `utils/array.ts:1-13` | N/A | No (Python has `itertools`/sets) | N/A | Skip. | DROP |
| 6 | Set helpers `difference / intersects / every / union` (hand-optimized for hot path). | `utils/set.ts:1-54` | N/A | No (Python `set` operators do this) | N/A | Skip. | DROP |
| 7 | `CircularBuffer<T>` — fixed-size ring buffer with `add/addAll/getRecent(n)/toArray/clear`, evicts oldest. | `utils/CircularBuffer.ts:1-85` | No (v4 keeps full message list, trims via `truncate_messages`) | Optional | Block A (compaction) or Block I (memory) | Useful for FRC (Function Result Clearing) — keep last N tool results uncached. Python: `collections.deque(maxlen=n)`. Good if v5 adds explicit FRC; optional if compaction handles it. | OPTIONAL |
| 8 | `createSignal<Args>()` — listener-set primitive: `subscribe / emit / clear`. ~8-line boilerplate replacement; ~15× duplicated across Runnable. | `utils/signal.ts:1-43` | No (v4 is single-threaded synchronous; no event-driven UI) | No | N/A | v4 chat.ipynb is canonical UI; no need for cross-component signals. Skip. | DROP |
| 9 | `createStore<T>(initial, onChange?)` — minimal store with `getState/setState/subscribe`, includes `Object.is`-shortcut. Total: 35 lines. | `state/store.ts:1-35` | No | No (single-user, no cross-component reactivity) | N/A | v4 mutates module-level globals. Reactive store only matters if v5 has multi-component UI; chat.ipynb cells are sequential. Skip unless we add an `AgentState` snapshot for `claude ps`-equivalent. | DROP |
| 10 | `memoizeWithTTL` / `memoizeWithTTLAsync` / `memoizeWithLRU` — write-through TTL cache + LRU cache + cold-miss dedup map (`inFlight`). Async version handles concurrent cold-miss correctly. | `utils/memoize.ts:1-269` | Partial — v4 uses `functools.lru_cache` ad-hoc | **Yes (subset)** | Block C (caching), Block H (memory) | Python ports needed:<br>• `lru_cache(maxsize=N)` covers LRU.<br>• TTL: write helper `cached_with_ttl(ttl_s, fn)` using `time.monotonic()`.<br>• In-flight dedup for async-style auth refresh: use `asyncio.Lock`/dict.<br>v4 already does similar in subagent token estimator; lift into a shared helper module. | RECOMMEND-ADD |
| 11 | `safeParseJSON` (memoized + LRU bounded + BOM strip + 8KB key cap) and `parseJSONL<T>` (Bun.JSONL.parseChunk with manual fallback) and `readJSONLFile<T>` (tail-100MB safe read). | `utils/json.ts:1-278`, `utils/jsonRead.ts:1-17` | Partial — v4 uses bare `json.loads` and persists transcripts as JSONL | **Yes (BOM-strip + memoize)** | Block H (sessionStorage) | Add `_safe_parse_json(s, log_errors=True)` with BOM-strip (PowerShell 5 writes UTF-8-BOM). Keep memoize bounded by `lru_cache(maxsize=50)` keyed on first 8KB. JSONL tail-read protects against multi-GB transcripts. | RECOMMEND-ADD |
| 12 | `toError / errorMessage / shortErrorStack / getErrnoCode / isENOENT / isFsInaccessible / classifyAxiosError`. ENOENT/EACCES/EPERM/ENOTDIR/ELOOP family. AxiosErrorKind = auth/timeout/network/http/other. | `utils/errors.ts:1-238` | Partial — v4 catches generic exceptions | **Yes** | Block L (errors) | Python equivalents: `_short_traceback(e, frames=5)`, `_is_fs_inaccessible(e)` (matches `FileNotFoundError`/`PermissionError`/`IsADirectoryError`/`OSError errno 40`), `_classify_http_error(e)` (botocore `ClientError` → auth/throttle/timeout/server/other). The 5-frame stack truncation is high-leverage: full tracebacks waste 500-2000 tool-result chars. | RECOMMEND-ADD |
| 13 | Error class hierarchy: `ClaudeError`, `MalformedCommandError`, `AbortError`, `ConfigParseError(filePath, defaultConfig)`, `ShellError(stdout, stderr, code, interrupted)`, `TelemetrySafeError_I_VERIFIED_THIS_IS_NOT_CODE_OR_FILEPATHS`. | `utils/errors.ts:3-101` | No (v4 raises generic Exception) | **Yes** | Block L | Add `class ShellError(Exception)` carrying `stdout/stderr/code/interrupted` + `class ConfigParseError(Exception)` carrying `path/default`. Improves recovery (auto-fall-back to default) without changing user surface. | RECOMMEND-ADD |
| 14 | `formatFileSize / formatDuration / formatSecondsShort / formatRelativeTimeAgo / formatTokens` — pure display helpers, locale-cached `Intl.NumberFormat`. | `utils/format.ts:1-309` | Partial — v4 has ad-hoc `f"{x/1024:.1f}KB"` strings scattered | Optional | Block E (rendering) | Centralize as `compact_v5/MAIN/agent/format.py` so HTML report + chat.ipynb + AGENT_STATUS use the same units. ~80 LOC. | RECOMMEND-ADD |
| 15 | `tokenCountWithEstimation(messages)` — canonical context-size measurement: walks back to last usage record, includes interleaved tool_results from same `message.id`. Fixes parallel-tool-call undercount (PR comment in body). | `utils/tokens.ts:226-261` | Partial — v4 estimates via char/4 in `_estimate_tokens` but doesn't anchor on usage iterations | **Yes** | Block B (token bookkeeping) | Lift the algorithm into Python: walk last assistant message → use Bedrock usage if present → estimate residual messages. Fixes a known v4 over/under-count when parallel tool calls are split across records. | RECOMMEND-ADD |
| 16 | `finalContextTokensFromLastResponse` — uses server-returned `usage.iterations[-1]` for budget countdown across compaction. Falls back to `input_tokens + output_tokens` when no iterations. | `utils/tokens.ts:79-112` | No | Optional | Block B | Bedrock returns top-level `usage` only — no `iterations`. So skip the iterations branch; the fallback formula is what v5 wants. Tiny patch, big correctness for token-budget UX. | RECOMMEND-ADD |
| 17 | `BufferedWriter` — sync writeFn + flush interval + maxBufferSize/Bytes + setImmediate-deferred overflow. Used by `debug.ts` to keep render loops cheap. | `utils/bufferedWriter.ts:1-101` | No (v4 writes audit log line-by-line) | Optional | Block H (audit log) / Block L (errors) | Python: `BufferedWriter` class with `threading.Timer` for periodic flush. Useful only if v5 has high-frequency log writes. v4's audit log is fine without it (low volume). | OPTIONAL |
| 18 | `cleanupRegistry` — global `Set<() => Promise<void>>` for graceful-shutdown cleanup. `registerCleanup(fn) → unregister`. Pure: 26 LOC. | `utils/cleanupRegistry.ts:1-26` | Partial — v4 has ad-hoc `try/finally` | **Yes** | Block 0 (bootstrap) or Block L | Python `atexit.register` + an explicit `cleanup_registry: list[Callable]` for SIGINT path. The pattern is correct because Python's atexit doesn't run on SIGTERM in all platforms — explicit registry is more reliable. ~15 LOC. | RECOMMEND-ADD |
| 19 | `createCombinedAbortSignal(signal, {signalB?, timeoutMs?})` + `createChildAbortController(parent)` (WeakRef-based, GC-safe). | `utils/combinedAbortSignal.ts:1-47`, `utils/abortController.ts:1-99` | No (v4 has no abort/cancel signal at all) | **Yes** | Block N (parallel tools) + Block A (compaction interrupt) | Python: pass `asyncio.Event()` or `concurrent.futures.CancelledError` paths. v4 currently can't cancel a long bash; user has to wait. Adding cancellation is a usability win and protects against runaway agents. | RECOMMEND-ADD |
| 20 | `runWithCwdOverride(cwd, fn)` via `AsyncLocalStorage` — concurrent agents see their own cwd without affecting each other. `pwd()` reads the AsyncLocalStorage store. | `utils/cwd.ts:1-33` | Partial — v4 uses os.chdir() globally | **Yes** | Block G (subagents) | Python: `contextvars.ContextVar('cwd')` + `cwd_var.set(...)` inside subagent task. Replaces global `os.chdir()` which is unsafe across asyncio tasks. ~20 LOC. | RECOMMEND-ADD |
| 21 | `generateTempFilePath(prefix, ext, {contentHash?})` — content-hash mode produces stable path across processes (so it doesn't bust prompt-cache when injected into tool descriptions). | `utils/tempfile.ts:1-32` | Partial — v4 uses `tempfile.NamedTemporaryFile` (random) | **Yes** | Block A (cache-aware compaction) | Critical pattern: any path/value injected into a tool description must be stable per-content, not random per-process, otherwise the cache prefix flips on every subagent spawn. v4 doesn't currently inject paths into descriptions, but the pattern should be in the toolkit before we add anything that does. | RECOMMEND-ADD |
| 22 | `generateWordSlug() / generateShortWordSlug()` — adjective-verb-noun via crypto.randomBytes; ~700 words; produces "gleaming-brewing-phoenix"-style memorable IDs. | `utils/words.ts:1-801` | No (v4 uses `uuid.uuid4()`) | Optional | Block H (session naming) | Pure UX: easier to refer to "calm-pondering-otter" than `7f3a-...-9def`. ~80 LOC port. Optional sweetener. | OPTIONAL |
| 23 | `validateUuid(s) → UUID | null` + `createAgentId(label?)` (format `a{label-}{16hex}`). Branded `SessionId`/`AgentId` types. | `utils/uuid.ts:1-28`, `types/ids.ts:1-45` | Partial — v4 uses raw `uuid.uuid4()` strings | Optional | Block G (subagent IDs) | Python lacks branded types but a NewType + helper `_make_agent_id(label=None) → str` (with `re.match` validator) gives the same authorial intent. Optional. | OPTIONAL |
| 24 | `parseFrontmatter(markdown, sourcePath?)` with auto-quoting of values containing YAML special chars (`{[*&#!|>%@`` ` `` or `: `). `splitPathInFrontmatter` with brace-pattern expansion. `coerceDescriptionToString` (scalar/null/array policy). `parseShellFrontmatter` (bash/powershell). | `utils/frontmatterParser.ts:1-371` | Partial — v4 skills have a custom md-frontmatter parser | **Yes** (better than v4) | Block J (skills loader) | Three additions to v4's skill loader: (a) auto-quote special-char values so glob patterns like `**/*.{ts,tsx}` parse, (b) brace expansion for `paths:` field, (c) graceful coercion of non-string description values. Each fixes a real failure mode. ~150 LOC port. | RECOMMEND-ADD |
| 25 | `parseYaml(input)` — uses `Bun.YAML.parse` if Bun present, else lazy-requires `yaml` package. | `utils/yaml.ts:1-16` | Yes (v4 uses PyYAML) | No (v4 already has it) | N/A | Skip. | DROP |
| 26 | `escapeXml(s)` / `escapeXmlAttr(s)` — pure escaping for safe interpolation into `<tag>${here}</tag>` and attributes. | `utils/xml.ts:1-17` | Partial — v4 builds tags manually, occasional bug class | **Yes** | Block I (memory/skills loader) + Block T (tool prompts) | Critical: v4 wraps content in `<tool_use>...</tool_use>` and similar without escaping. If a user's CLAUDE.md or memory contains literal `<` or `&`, it can corrupt the prompt structure. ~10 LOC port. | RECOMMEND-ADD |
| 27 | XML tag constants (`COMMAND_NAME_TAG`, `BASH_INPUT_TAG`, `TASK_NOTIFICATION_TAG`, `FORK_BOILERPLATE_TAG`, `TICK_TAG`, etc.) + `TERMINAL_OUTPUT_TAGS` array + `COMMON_HELP_ARGS` / `COMMON_INFO_ARGS` arg patterns. | `constants/xml.ts:1-87` | Partial — v4 hardcodes some tag names inline | Optional | Block T | Centralize tag names as Python constants so they don't drift. The `COMMON_HELP_ARGS = ['help', '-h', '--help']` and `COMMON_INFO_ARGS` arrays are useful for slash-command short-circuit. ~30 LOC. | OPTIONAL |
| 28 | `BINARY_EXTENSIONS` set (60+ extensions across Images/Videos/Audio/Archives/Executables/Docs/Fonts/Bytecode/DBs/Design/Flash) + `hasBinaryExtension(path)` + `isBinaryContent(buffer)` (null-byte sniff + 10% non-printable threshold over first 8KB). | `constants/files.ts:1-156` | Partial — v4 has narrower binary detection | **Yes** | Block T (FileReadTool, GrepTool) | v4 currently fails ungracefully when reading binary files (e.g. .pyc, .so). Lift the full extension set + 8KB null-byte heuristic. ~50 LOC port. Prevents waste of context on binary content. | RECOMMEND-ADD |
| 29 | `API_IMAGE_MAX_BASE64_SIZE` (5MB) / `IMAGE_TARGET_RAW_SIZE` (3.75MB) / `IMAGE_MAX_WIDTH/HEIGHT` (2000) / `PDF_TARGET_RAW_SIZE` (20MB) / `API_PDF_MAX_PAGES` (100) / `API_MAX_MEDIA_PER_REQUEST` (100) / `PDF_MAX_PAGES_PER_READ` (20) / `PDF_AT_MENTION_INLINE_THRESHOLD` (10). | `constants/apiLimits.ts:1-95` | Partial — v4 hardcodes a few of these | **Yes** | Block T (image/PDF tools) | All Anthropic-API-side hard limits. Same constants apply on Bedrock (server-side enforcement). v4 should fail-fast client-side instead of getting confusing 400s. ~20 LOC. | RECOMMEND-ADD |
| 30 | `DEFAULT_MAX_RESULT_SIZE_CHARS = 50_000` / `MAX_TOOL_RESULT_TOKENS = 100_000` / `BYTES_PER_TOKEN = 4` / `MAX_TOOL_RESULTS_PER_MESSAGE_CHARS = 200_000` / `TOOL_SUMMARY_MAX_LENGTH = 50`. | `constants/toolLimits.ts:1-57` | Partial — v4 has its own limits but not symmetric | **Yes** | Block T + Block A | Adopt the Runnable values. The per-message budget (200k chars) is a NEW concept v4 lacks: parallel tool calls in one turn collectively bounded, not just per-tool. Useful when adding parallel tools (Block N). | RECOMMEND-ADD |
| 31 | `getDefaultBashTimeoutMs(env=process.env)` (default 120s) / `getMaxBashTimeoutMs` (default 600s, max ≥ default) — env-var-driven via `BASH_DEFAULT_TIMEOUT_MS` / `BASH_MAX_TIMEOUT_MS`. | `utils/timeouts.ts:1-40` | Yes (hardcoded `BASH_TIMEOUT_S = 120`) | Optional | Block T (bash tool) | Adopt the env-var override pattern so power users can extend without forking the agent. ~15 LOC. | OPTIONAL |
| 32 | `validateBoundedIntEnvVar(name, value, defaultValue, upperLimit)` returns `{effective, status: 'valid'|'capped'|'invalid', message?}` with debug logging. | `utils/envValidation.ts:1-39` | No | **Yes** | Block 0 (bootstrap) | Single helper for sanitizing all numeric env vars (timeouts, max retries, cache sizes). Replaces 5+ ad-hoc parse-and-clamp blocks scattered in v4. ~30 LOC. | RECOMMEND-ADD |
| 33 | `Signal<Args>` + `subscribeClassifierChecking` ephemeral state map for tool-use approval state across components. | `utils/classifierApprovals.ts:1-89` | No | No (v4 has no parallel tool approval UI) | N/A | UI-driven; chat.ipynb shows results sequentially. Skip. | DROP |
| 34 | `MODEL_COSTS` table with tiers `COST_TIER_3_15` (Sonnet $3/$15) / `COST_TIER_15_75` (Opus 4/4.1 $15/$75) / `COST_TIER_5_25` (Opus 4.5/4.6 $5/$25) / `COST_TIER_30_150` (fast-mode Opus $30/$150) / `COST_HAIKU_35` ($0.80/$4) / `COST_HAIKU_45` ($1/$5). `calculateUSDCost(model, usage)` + `formatModelPricing`. Server-tool-use web search at $0.01/req. | `utils/modelCost.ts:1-232` | Partial — v4 has Haiku 4.5 cost tier but not the full table | **Yes (Haiku 4.5 + Sonnet 4.6 only)** | Block B (cost tracking) | Single Bedrock-relevant tier table. Drop fast-mode (Bedrock doesn't expose `usage.speed`). v5 only ever sees `claude-haiku-4-5-*` and `claude-sonnet-4-6-*` (per system_prompt env), so a 2-row table suffices. Keep the formatPrice helper. ~40 LOC. | RECOMMEND-ADD |
| 35 | `readFileInRange(path, offset, maxLines, maxBytes, signal, {truncateOnByteLimit?})` — fast path under 10MB (whole-file readFile + indexOf-split), streaming path otherwise. BOM strip, CRLF normalization, optional byte-cap mode that stops at last complete line. `FileTooLargeError(size, max)`. | `utils/readFileInRange.ts:1-383` | Partial — v4 reads whole file, no offset/limit, no streaming | **Yes** | Block T (FileReadTool) | This is the canonical "Read N lines starting at line K of a giant file" pattern. v4's FileReadTool can OOM on multi-GB files. Lift the fast/streaming dichotomy as Python (use `aiofiles` or sync `open` with `seek`). Big correctness improvement for `Read` on large logs. ~250 LOC port. | RECOMMEND-ADD |
| 36 | `lockfile` lazy wrapper — defers loading `proper-lockfile` (which imports `graceful-fs` and monkey-patches all of fs, ~8ms startup cost) until the first lock call. | `utils/lockfile.ts:1-44` | No (v4 has no file-lock pattern) | Optional | Block H (session storage on shared FS) | SageMaker Studio is single-user but session JSONL can be opened from chat.ipynb + a notebook in parallel. Python: `fcntl.flock` (Linux) or `portalocker`. Cheap insurance. ~30 LOC. | OPTIONAL |
| 37 | `SYSTEM_PROMPT_DYNAMIC_BOUNDARY` marker + `systemPromptSection(name, compute)` (memoized) + `DANGEROUS_uncachedSystemPromptSection(name, compute, reason)` (recomputes every turn) + `resolveSystemPromptSections` cache lookup. | `constants/systemPromptSections.ts:1-69` + `constants/prompts.ts:107-577` | Partial — v4 builds system prompt with f-strings, no static/dynamic boundary | **Yes** | Block 0 + Block A (cache stability) | This is THE prompt-cache stability pattern. Static prefix (cacheable across orgs/sessions) + boundary marker + dynamic suffix. v4 currently rebuilds the prompt on every turn — anything mutable (memory hash, env info) busts the cache prefix. Adopt the section-with-cacheBreak-flag model: static sections returns `(name, compute)`, volatile sections require explicit DANGEROUS marker + reason string. Forces every cache-busting change to be deliberate. ~80 LOC + refactor of `_build_system_prompt`. **High leverage**. | RECOMMEND-ADD |
| 38 | `SUMMARIZE_TOOL_RESULTS_SECTION` — exact prompt string telling the model to write down important info before tool results get cleared. Pairs with FRC. | `constants/prompts.ts:841` | No | Optional | Block A (FRC) | Only matters if v5 adopts Function Result Clearing. The string itself is small but battle-tested. | OPTIONAL |
| 39 | `getKnowledgeCutoff(modelId)` — Sonnet 4.6 → August 2025, Opus 4.6 → May 2025, Opus 4.5 → May 2025, Haiku 4 → February 2025, older Claude 4 → January 2025. | `constants/prompts.ts:712-730` | No (v4 lets model guess) | **Yes** | Block 0 (env info) | Adding "Assistant knowledge cutoff is X" to the env block prevents the model from claiming it doesn't know recent events when it does, and from confidently asserting recency it doesn't have. Two-line helper. | RECOMMEND-ADD |
| 40 | `getCLISyspromptPrefix({isNonInteractive, hasAppendSystemPrompt})` — chooses between `"You are Claude Code, Anthropic's official CLI"` / `"running within the Claude Agent SDK"` / `"You are a Claude agent built on Anthropic's Claude Agent SDK"`. Vertex always returns DEFAULT. | `constants/system.ts:10-46` | Partial — v4 has a single hardcoded prefix | Optional | Block 0 | Useful only if v5 has multiple invocation modes. Single-mode v5 keeps one string. | OPTIONAL |
| 41 | `BLACK_CIRCLE / TEARDROP_ASTERISK / DIAMOND_OPEN / DIAMOND_FILLED / FORK_GLYPH / BLOCKQUOTE_BAR / HEAVY_HORIZONTAL / BRIDGE_SPINNER_FRAMES / EFFORT_LOW/MEDIUM/HIGH/MAX` and platform-conditional `BLACK_CIRCLE = darwin ? '⏺' : '●'` etc. | `constants/figures.ts:1-46` | Partial — v4 uses `●` directly | Optional | Block E (rendering) | Useful for HTML report aesthetics. Python: small dict keyed by symbol name. ~20 LOC. | OPTIONAL |
| 42 | `SPINNER_VERBS` (200 verbs) + `TURN_COMPLETION_VERBS` (8 past-tense). | `constants/spinnerVerbs.ts:1-205`, `constants/turnCompletionVerbs.ts:1-13` | No | Optional | Block E | UX sweetener for AGENT_STATUS in chat.ipynb. ~10 LOC port. | OPTIONAL |
| 43 | `prependBullets(items: Array<string \| string[]>)` — flatten with ` - ` for top-level and `   - ` for sub-bullets. | `constants/prompts.ts:167-172` | No | Optional | Block 0 (system prompt builder) | Tiny helper that makes nested list construction in prompts readable. ~5 LOC. | OPTIONAL |
| 44 | `parseSlashCommand(input)` — handles `/cmd args`, `/cmd:sub args`, `/mcp:tool (MCP) args`. Returns `{commandName, args, isMcp}` or null. | `utils/slashCommandParsing.ts:1-60` | Partial — v4 has its own slash parser | Optional | Block D (commands) | Adopt the (MCP)-suffix detection pattern even if MCP itself is dropped — future-proofs the namespace. ~20 LOC. | OPTIONAL |
| 45 | `substituteArguments(content, args, appendIfNoPlaceholder, argumentNames)` — `$ARGUMENTS`, `$ARGUMENTS[0..N]`, `$0..$N` shorthand, named `$foo` placeholders. Uses shell-quote parser. `parseArguments` / `parseArgumentNames` / `generateProgressiveArgumentHint`. | `utils/argumentSubstitution.ts:1-145` | Partial — v4 supports `$ARGUMENTS` only | **Yes** | Block J (skills/commands) | Three real improvements over v4: indexed `$ARGUMENTS[0]`, shorthand `$0`, named args from frontmatter. Backwards-compatible (still supports bare `$ARGUMENTS`). ~80 LOC port (Python regex). | RECOMMEND-ADD |
| 46 | `EFFORT_LEVELS = ['low', 'medium', 'high', 'max']` + `parseEffortValue` (accepts numbers + strings) + `convertEffortValueToLevel(numeric → level)` + `resolveAppliedEffort(model, effortValue)` (env override → app state → model default, downgrades 'max' on non-Opus-4.6 to 'high'). | `utils/effort.ts:1-329` | No | No (Bedrock doesn't expose effort yet) | N/A | Effort/thinking_budget is firstParty-only ATM. Skip until Bedrock adds parity. | DROP |
| 47 | `parseFrontmatter`-derived `FrontmatterShell = 'bash' | 'powershell'` + `parseShellFrontmatter` validator. | `utils/frontmatterParser.ts:339-370` | No | Optional | Block J (skills) | Useful only if skills can shell-exec in the loader. v4 doesn't currently. Skip unless v5 adds shell-block execution in skill loader. | DROP |
| 48 | `parseJSONLBuffer` / `parseJSONLString` / Bun's `Bun.JSONL.parseChunk` fallback chain — skips malformed lines. | `utils/json.ts:128-190` | Partial — v4 uses `for line in file: try: json.loads`. Same effect, different implementation | No (v4 already idiomatic) | N/A | Skip. | DROP |
| 49 | `addItemToJSONCArray(content, newItem)` — preserves comments + formatting via `jsonc-parser` `modify()` + `applyEdits`. | `utils/json.ts:228-277` | No | No (v4 doesn't write JSONC) | N/A | Settings.json is JSONC for VS Code; v5 can use plain JSON. Skip. | DROP |
| 50 | `parsePositiveIntFromFrontmatter / parseBooleanFrontmatter / coerceDescriptionToString` — graceful frontmatter scalar coercion. | `utils/frontmatterParser.ts:275-335` | Partial — v4 frontmatter parser is strict | **Yes** | Block J | Same package as #24. Adopt together. | RECOMMEND-ADD (with #24) |
| 51 | `getLogDisplayTitle(log, defaultTitle?)` — fallback chain: agentName → customTitle → summary → firstPrompt (skipped if `<tick>...`) → defaultTitle → "Autonomous session" → first 8 chars of sessionId. + `dateToFilename(date)`. | `utils/log.ts:30-58` | Partial — v4 uses firstPrompt directly | Optional | Block H (session listing) | Improves /resume UX when sessions have AI-generated titles or empty firstPrompts (`/clear` only). Optional polish. | OPTIONAL |
| 52 | `getInMemoryErrors() / addToInMemoryErrorLog` — bounded ring buffer (100 entries) of recent errors for bug-report inclusion. | `utils/log.ts:65-77, 200-203` | No | Optional | Block L | Useful for `/share` / `/issue` flow. ~20 LOC. | OPTIONAL |
| 53 | `LRUCache`-based memoize for `safeParseJSON` with 8KB key cap (skips caching big inputs to avoid `keyList` ballooning). | `utils/json.ts:42-58` | No | Optional | Block C | The 8KB-key threshold is the load-bearing detail — caching very large JSON-string keys is what caused the original 300MB+ leak. ~10 LOC delta to existing memoize. | OPTIONAL |
| 54 | `attachErrorLogSink` — defer sink wiring; queue events before sink attaches; drain queue on attach. | `utils/log.ts:99-134` | No | Optional | Block 0 (bootstrap) | Solves "module loaded before logging configured" startup bug. v4 has the same problem latent. ~30 LOC. | OPTIONAL |
| 55 | `Entry` discriminated union for transcript lines: `TranscriptMessage | SummaryMessage | CustomTitleMessage | AiTitleMessage | LastPromptMessage | TaskSummaryMessage | TagMessage | AgentNameMessage | AgentColorMessage | AgentSettingMessage | PRLinkMessage | FileHistorySnapshotMessage | AttributionSnapshotMessage | QueueOperationMessage | SpeculationAcceptMessage | ModeEntry | WorktreeStateEntry | ContentReplacementEntry | ContextCollapseCommitEntry | ContextCollapseSnapshotEntry`. + `sortLogs(logs)` (modified-desc, then created-desc). | `types/logs.ts:55-330` | Partial — v4 stores only `{role, content}` per line + summary | **Yes (subset)** | Block H (sessionStorage) | v5 transcript needs at minimum: `TranscriptMessage`, `SummaryMessage`, `CustomTitleMessage`, `TaskSummaryMessage`. Each is a few fields. Adopting the discriminated-type pattern (Python: `Literal['summary'] type` field) future-proofs the format and allows incremental additions without breaking existing transcripts. **High leverage** for resume + listing UX. | RECOMMEND-ADD |
| 56 | `PermissionDecision<Input>` discriminated union (allow/ask/deny) + `PermissionResult` with `passthrough` extension + `PermissionDecisionReason` 10-variant union (rule/mode/subcommandResults/permissionPromptTool/hook/asyncAgent/sandboxOverride/classifier/workingDir/safetyCheck/other). | `types/permissions.ts:140-324` | Partial — v4 uses `'allow' | 'deny' | 'ask'` strings only | Optional | Block N (permission system) | Useful if v5 expands beyond v4's 3-state model. The `decisionReason` field (with rich subtypes) is what enables auditability ("why was this allowed?"). Adopt the structure even if not all variants are implemented. ~80 LOC port. | OPTIONAL |
| 57 | `EXTERNAL_PERMISSION_MODES = ['acceptEdits', 'bypassPermissions', 'default', 'dontAsk', 'plan']` + internal-only 'auto' / 'bubble'. `INTERNAL_PERMISSION_MODES` includes 'auto' only when classifier feature is on. | `types/permissions.ts:16-39` | Partial — v4 has fewer modes | Optional | Block N | Adopt at least 'plan' mode (read-only exploration) and 'acceptEdits' (auto-approve file edits in current dir). Two big UX wins. | OPTIONAL |
| 58 | `HooksSchema` (Zod) — `partialRecord(HOOK_EVENTS, HookMatcher[])`. `HookCommandSchema` discriminated on `type: 'command' | 'prompt' | 'http' | 'agent'`. `if` filter using permission-rule syntax (`Bash(git *)`). `once / async / asyncRewake / statusMessage` per-hook. | `schemas/hooks.ts:1-223` | No | No (v5 plugins/hooks are out-of-scope) | N/A | Hooks are settings-driven plugin system; v5 single-user has no users to configure them. Skip. | DROP |
| 59 | `HookCallback` (in-process callback hook) + `HookCallbackContext` (`getAppState`, `updateAttributionState`) + `HookResult` / `AggregatedHookResult` shapes (outcome: success/blocking/non_blocking_error/cancelled). | `types/hooks.ts:202-291` | No | Optional | Block N (parallel tool execution) | The CALLBACK form (not the settings form) is interesting: a Python registry of `pre_tool_use(tool_name, input)` and `post_tool_use(tool_name, result)` callbacks would cleanly express things v4 currently inlines (audit log, telemetry, permission checks). Optional refactor. | OPTIONAL |
| 60 | `TaskSummaryMessage` — periodic fork-generated summary of agent activity, persisted every min(5 steps, 2min). Used by `claude ps` to show useful status instead of last user prompt. | `types/logs.ts:88-98` | No | Optional | Block H (status) | Useful only if v5 has a way to introspect ongoing sessions externally. SageMaker chat.ipynb already shows status in the notebook itself. Skip. | DROP |
| 61 | `Migration` pattern — read userSettings → conditional rewrite → mark complete in globalConfig. Always idempotent. Always reads ONLY userSettings (never merged) so project/policy pins aren't promoted. Examples: `migrateAutoUpdatesToSettings`, `migrateBypassPermissionsAcceptedToSettings`, `migrateOpusToOpus1m`, `migrateSonnet45ToSonnet46`, `migrateLegacyOpusToCurrent`. | `migrations/*.ts` (11 files) | No | Optional | Block 0 (bootstrap) | Pattern is reusable: every settings/config rename should follow `(read user-only) → (rewrite if matches old form) → (mark complete; idempotent on re-run)`. Useful when v5 evolves config beyond v4. The 11 specific migrations are not relevant (they migrate Anthropic-specific model aliases). Save as `_design_pattern.md` in synthesis/. | OPTIONAL |
| 62 | `getDefaultAppState()` — exhaustive default-construction of `AppState` (~150 fields). Demonstrates "every field has an explicit default" discipline. | `state/AppStateStore.ts:456-569` | No (v4 globals are scattered) | Optional | Block 0 | Pattern: a single dataclass `@dataclass(slots=True) class AgentState` with field defaults, constructed once at boot. Replaces v4's ~30 module-level globals. Refactor cost ~200 LOC. | OPTIONAL |
| 63 | `onChangeAppState({newState, oldState})` choke-point — single place that propagates state changes to side-effects (CCR sync, settings save, env var apply, auth cache clear). Catches all 8 mutation paths via diff, not via per-callsite invocation. | `state/onChangeAppState.ts:43-171` | No | **Yes** | Block 0 + Block A | The pattern itself is the win: any time state has multiple mutation points and side-effects must fire, route ALL through `_set_state(updater)` which compares before/after and triggers handlers. Stops the "we forgot to clear the auth cache when settings changed" bug class. ~50 LOC port. | RECOMMEND-ADD |
| 64 | `getViewedTeammateTask` / `getActiveAgentForInput` — pure selectors over AppState. Discriminated return: `{type:'leader'} | {type:'viewed', task} | {type:'named_agent', task}`. | `state/selectors.ts:1-77` | No | No | N/A | Multi-agent UI selectors. v5 single-user N/A. Skip. | DROP |
| 65 | `enterTeammateView / exitTeammateView / stopOrDismissAgent` — task lifecycle helpers (retain/release pattern with `evictAfter` grace period). | `state/teammateViewHelpers.ts:1-142` | No | No | N/A | Multi-agent UI. Skip. | DROP |
| 66 | `LRUCache` (lru-cache) used everywhere; `WeakMap`-based `zodToJsonSchema` cache (~60-250 calls/turn collapsed to one). | `utils/zodToJsonSchema.ts:1-23`, `utils/memoize.ts:243+` | No (v4 uses functools.lru_cache narrowly) | Optional | Block C | Pattern is correct: tool schema → JSON Schema is hot-path; weak-key cache (Python: `weakref.WeakValueDictionary`) makes it free. Adopt only if v5 uses pydantic/zod-like schema model. | OPTIONAL |
| 67 | `getLocalISODate()` (with `CLAUDE_CODE_OVERRIDE_DATE` ant override) + `getSessionStartDate` (memoized at session start, deliberately stale post-midnight to preserve cache). + `getLocalMonthYear()` (changes monthly, not daily, used in tool prompts). | `constants/common.ts:1-34` | Partial — v4 uses `datetime.now()` ad-hoc | **Yes** | Block 0 + tool prompts | Critical for cache stability: any tool prompt that mentions "today is X" must use `getLocalMonthYear()` not `getLocalISODate()` — otherwise the prompt cache busts at midnight. v4 doesn't currently inject dates into tool prompts so this is forward-looking. Memoize once at boot. ~15 LOC. | RECOMMEND-ADD |
| 68 | `formatModelPricing(costs)` + `getModelPricingString(model)` — `"$1/$5 per Mtok"`-style formatting with integer-vs-decimal handling (`$3` not `$3.00`, `$0.80` not `$0.8`). | `utils/modelCost.ts:204-231` | Partial — v4 has cost printing in audit log | Optional | Block B | Lift the integer-detection rule. ~10 LOC. | OPTIONAL |
| 69 | `getOauthConfig()` returns prod/staging/local config; `ALLOWED_OAUTH_BASE_URLS` allowlist for FedStart/PubSec deployment override. | `constants/oauth.ts:1-235` | No | No (Bedrock auth uses IAM role, not OAuth) | N/A | Skip. | DROP |
| 70 | `WORKFLOW_CONTENT` / `PR_BODY` / `CODE_REVIEW_PLUGIN_WORKFLOW_CONTENT` — GitHub Actions YAML templates. | `constants/github-app.ts:1-145` | No | No | N/A | Skip. | DROP |
| 71 | `CYBER_RISK_INSTRUCTION` — "IMPORTANT: Assist with authorized security testing... Refuse requests for destructive techniques...". Owned by Safeguards team, never edit without review. | `constants/cyberRiskInstruction.ts:1-24` | No | **Yes** | Block 0 (system prompt) | Drop into the system prompt verbatim. Adds a lightweight cyber-risk safety prefix without requiring v5 to design its own. Single string. | RECOMMEND-ADD |
| 72 | `OUTPUT_STYLE_CONFIG` — built-in 'Explanatory' (insights box) and 'Learning' (TODO(human) collaborative prompting) modes. Each is a full system-prompt suffix string. | `constants/outputStyles.ts:1-217` | No | Optional | Block J (skills) | The 'Learning' mode is an interesting prompt-engineering pattern (model adds `TODO(human)`, asks user to fill 2-10 lines). Worth recording in the prompt-engineering doc but not needed in v5. | OPTIONAL |
| 73 | `attribution header` — `x-anthropic-billing-header: cc_version=...; cc_entrypoint=...; cch=00000;` with native attestation token swap. | `constants/system.ts:73-95` | No | No (Bedrock doesn't accept this header) | N/A | Skip. | DROP |
| 74 | `BEDROCK_EXTRA_PARAMS_HEADERS` Set — betas that go in `extraBodyParams` not headers on Bedrock: `INTERLEAVED_THINKING_BETA_HEADER`, `CONTEXT_1M_BETA_HEADER`, `TOOL_SEARCH_BETA_HEADER_3P`. | `constants/betas.ts:38-43`, `utils/betas.ts:373-377` | Partial — v4 lives entirely on Bedrock but doesn't have a clean separation | **Yes** | Block 0 (Bedrock invocation) | This is the load-bearing constraint: betas like interleaved-thinking and context-1m REQUIRE going through `extraBodyParams` on Bedrock, NOT request headers. Adopt the explicit Set so v5 never accidentally puts a beta in the wrong place. ~5 LOC. | RECOMMEND-ADD |
| 75 | `getMergedBetas(model, {isAgenticQuery?})` — combines model-default betas with SDK user-provided betas, dedup, special-cases Haiku (excludes claude-code-20250219 unless agentic). | `utils/betas.ts:397-428` | No | Optional | Block 0 | Useful only if v5 ever exposes "let user request additional betas". Keep simple for now. | OPTIONAL |
| 76 | `getCwd()` / `pwd()` / `getWorktreeSession()` integration — env block in system prompt: `<env>...</env>` with cwd, git-or-not, additional dirs, platform, shell-info-line, OS version. + Windows shell hint: `"use Unix shell syntax, not Windows — e.g., /dev/null not NUL, forward slashes in paths"`. | `constants/prompts.ts:606-756` | Partial — v4 emits a similar block, slightly different format | **Yes** | Block 0 | Adopt the exact format including the Windows shell hint, OS version (uname -sr), and "Notes" appendix (`Agent threads always have their cwd reset between bash calls...`). v4 misses some of this. ~30 LOC. | RECOMMEND-ADD |
| 77 | `getScratchpadInstructions()` — "Always use this scratchpad directory for temporary files instead of /tmp" with the resolved per-session path inlined. Session-isolated, no permission prompts. | `constants/prompts.ts:797-819` | No | **Yes** | Block 0 + Block T (file tools) | A per-session scratchpad dir (e.g., `/tmp/claude_session_<id>/`) that's pre-allowlisted gets the model out of `/tmp` and lets v5 garbage-collect at session end. Writeup in prompt + auto-mkdir + cleanup hook. ~30 LOC. | RECOMMEND-ADD |
| 78 | `DEFAULT_AGENT_PROMPT` — string used by every spawned subagent: `"You are an agent for Claude Code... Complete the task fully—don't gold-plate, but don't leave it half-done. When you complete the task, respond with a concise report covering what was done and any key findings — the caller will relay this to the user, so it only needs the essentials."` | `constants/prompts.ts:758` | Partial — v4 has its own subagent prompt | **Yes** | Block G (subagents) | The exact phrasing of "don't gold-plate, but don't leave it half-done" + "concise report" + "caller will relay" is battle-tested. Adopt verbatim. | RECOMMEND-ADD |
| 79 | `enhanceSystemPromptWithEnvDetails`'s "Notes" appendix — `Agent threads always have their cwd reset between bash calls, as a result please only use absolute file paths.` + `share file paths (always absolute, never relative)` + `Include code snippets only when the exact text is load-bearing` + `For clear communication... avoid using emojis.` + `Do not use a colon before tool calls.` | `constants/prompts.ts:766-770` | Partial | **Yes** | Block G (subagents) | Each line fixes a specific failure mode observed in production. The "no colon before tool call" line in particular prevents the model from wasting tokens on `"Let me read the file:"`. Adopt verbatim. | RECOMMEND-ADD |
| 80 | `isHumanTurn(m)` — `m.type === 'user' && !m.isMeta && m.toolUseResult === undefined`. Three independent PRs fixed miscounts from checking `type==='user'` alone. | `utils/messagePredicates.ts:1-8` | Partial — v4 conflates user messages with tool results in some counters | **Yes** | Block A (compaction triggers) + Block H (history listing) | One-liner that prevents a recurring bug class. | RECOMMEND-ADD |
| 81 | `taggedId.toTaggedId(tag, uuid)` — base58 encoding of UUID-as-128-bit-int, format `user_01PaGUP2rbg1XDh7Z9W1CEpd`. Stable matches API's tagged_id.py. | `utils/taggedId.ts:1-54` | No | No (Bedrock doesn't use these IDs) | N/A | Skip. | DROP |
| 82 | `hyperlink.createHyperlink(url, content?)` — OSC 8 escape sequence for clickable terminal links, with `chalk.blue` color, falls back to plain text. | `utils/hyperlink.ts:1-39` | No | Optional | Block E (rendering) | chat.ipynb is a notebook, not a terminal — Markdown links work natively. Skip. | DROP |
| 83 | `linkifyIssueReferences` — replace `owner/repo#123` with OSC 8 hyperlink to GitHub. | `utils/markdown.ts:289-308` | No | Optional | Block E | Notebook handles markdown natively. Skip. | DROP |
| 84 | `applyMarkdown(content, theme, highlight)` — full marked.js + chalk + tables + lists + roman/alpha numerals at depth + `linkifyIssueReferences` + `BLOCKQUOTE_BAR` for blockquotes. Disables `del` (strikethrough) parser because model uses `~100` for "approximately". | `utils/markdown.ts:1-381` | N/A | No (notebook handles markdown) | N/A | Skip. | DROP |
| 85 | `padAligned(content, displayWidth, targetWidth, align)` + `getListNumber(depth, n)` (1, 2, 3 → a, b, c → i, ii, iii) + `numberToLetter` / `numberToRoman`. | `utils/markdown.ts:310-380` | N/A | No | N/A | Skip. | DROP |
| 86 | Plan slug system: `getPlanSlug(sessionId)` → up to 10 retries to find unused word-slug → cached per session. `getPlanFilePath(agentId?)` → main session uses `{slug}.md`, subagents use `{slug}-agent-{agentId}.md`. `copyPlanForResume / copyPlanForFork`. `persistFileSnapshotIfRemote()` (writes file_snapshot system messages into transcript when CCR is in use, since CCR doesn't persist files). `recoverPlanFromMessages` (extracts plan from ExitPlanMode tool_use input, planContent on user messages, or plan_file_reference attachment). | `utils/plans.ts:1-398` | No | Optional | Block J (plan mode) | Plan mode is OUT-OF-SCOPE per v5 constraints (single-user, no plan/exec separation), but the **slug-naming pattern** is reusable: each subagent gets a memorable directory name. Worth ~30 LOC if v5 has subagents. | OPTIONAL |
| 87 | `FpsTracker` class — record render durations, compute average FPS + 1%-low (p99 frame time → 1000/p99). | `utils/fpsTracker.ts:1-48` | N/A | No (notebook UI) | N/A | Skip. | DROP |
| 88 | `JETBRAINS_IDES` array (15 IDEs) + `detectTerminal()` — exhaustive 60+-condition terminal detection (Cursor, VS Code, Windsurf, Antigravity, Codium, JetBrains, Visual Studio, Ghostty, Kitty, tmux, screen, Konsole, GNOME Terminal, xterm, VTE, terminator, alacritty, tilix, Windows Terminal, cygwin, MSYS, ConEmu, WSL, SSH, etc.). + `detectDeploymentEnvironment()` — Codespaces/Gitpod/Replit/Glitch/Vercel/Railway/Render/Netlify/Heroku/Fly/CF Pages/Deno Deploy/Lambda/Fargate/ECS/EC2/Cloud Run/GCP/Azure/Hugging Face Spaces/GitHub Actions/GitLab CI/CircleCI/Buildkite/CI/Kubernetes/Docker. | `utils/env.ts:115-305` | Partial — v4 has narrow detection | Optional | Block 0 (analytics-style detection) | The Cloud Run / Lambda / Kubernetes / Docker detection is genuinely useful for v5 (`detectDeploymentEnvironment() → 'sagemaker'` could go in env block). Add a SageMaker-specific detector by checking `SM_HOME`/`SM_CHANNEL_*` env vars, plus the Docker/Kubernetes detection. ~50 LOC port. | OPTIONAL |
| 89 | `BufferedWriter` immediate-mode flag — when `--debug` is on, writes are sync (`appendFileSync`) so they aren't lost on `process.exit()` or hang `beforeExit`. Otherwise async-buffered with 1-second flush. | `utils/debug.ts:155-197` | Partial — v4 logs are sync but unbuffered | Optional | Block L | Useful pattern: `immediateMode = (debug_enabled or process.exiting)`, otherwise buffer. Avoids losing the last log line on crash. | OPTIONAL |
| 90 | `--debug=pattern` CLI parsing — `parseDebugFilter` + `shouldShowDebugMessage(message, filter)`. Per-call filtering. | `utils/debug.ts:73-83`, `debugFilter.ts` (not read) | No | Optional | Block L | Useful for `--debug=cache` etc. Optional. | OPTIONAL |
| 91 | `setHasFormattedOutput(true)` — switches `logForDebugging` to JSON-stringify multi-line messages so JSONL output stays parseable. | `utils/debug.ts:127-133, 215-219` | No | Optional | Block L | Tiny correctness fix for log file consumers. | OPTIONAL |
| 92 | `FORK_BOILERPLATE_TAG` + `FORK_DIRECTIVE_PREFIX = 'Your directive: '` — XML wrapping of subagent boilerplate so transcript renderer can collapse to just the directive. | `constants/xml.ts:62-66` | No | No | N/A | Renderer-specific; chat.ipynb doesn't need this. Skip. | DROP |
| 93 | `BuiltinPluginDefinition` shape with `isAvailable() / defaultEnabled / skills / hooks / mcpServers`. + `LoadedPlugin` shape with `commandsPath / agentsPath / skillsPath / outputStylesPath / hooksConfig / mcpServers / lspServers`. | `types/plugin.ts:1-70` | No | No | N/A | Plugins out-of-scope. Skip. | DROP |
| 94 | `PluginError` discriminated union (28 variants) + `getPluginErrorMessage` formatter — exhaustive error categorization with field-level context per variant. Examples: `'mcpb-download-failed'`, `'lsp-server-crashed' { exitCode, signal? }`, `'marketplace-blocked-by-policy' { blockedByBlocklist?, allowedSources }`, `'dependency-unsatisfied' { dependency, reason: 'not-enabled' | 'not-found' }`. | `types/plugin.ts:79-363` | No | Optional (pattern only) | Block L | The PATTERN is high-leverage even though plugins are skipped: every error class should be a discriminated type with structured fields, not a string match. Apply same to `ToolError` family in v5: `{type: 'file-too-large', size, max, path} | {type: 'binary-file', path} | {type: 'denied-by-permission', rule, suggestion}` etc. The formatter pattern (`switch on type, format with field interpolation`) cleanly separates structured error from rendering. **High-leverage style guide**. | RECOMMEND-ADD (as design pattern) |
| 95 | `PromptCommand` shape: `{type:'prompt', progressMessage, contentLength, argNames?, allowedTools?, model?, source, hooks?, skillRoot?, context?: 'inline'|'fork', agent?, effort?, paths?, getPromptForCommand}`. `LocalCommand`/`LocalJSXCommand` types. | `types/command.ts:25-216` | Partial — v4 has skills but not the `context: 'inline' | 'fork'` distinction | **Yes** | Block J (skills) | The `context: 'inline' | 'fork'` field is the key idea: a skill can either expand into the current conversation OR run as a separate sub-agent with its own context budget. v4 only has inline. Adding 'fork' mode gives long skill prompts their own token budget without polluting the main conversation. The `paths?: string[]` field (skill only visible after model touches matching files) is also new. ~40 LOC port + skill loader changes. | RECOMMEND-ADD |
| 96 | `CommandAvailability = 'claude-ai' | 'console'` — declares which auth/provider gating shows a command. Distinct from `isEnabled()`: availability=who-can-see, isEnabled=is-it-on-now. | `types/command.ts:165-203` | No | No (Bedrock-only, single-user) | N/A | Skip. | DROP |
| 97 | `CommandBase` fields: `name / description / aliases / isMcp / argumentHint / whenToUse / version / disableModelInvocation / userInvocable / loadedFrom: 'commands_DEPRECATED' | 'skills' | 'plugin' | 'managed' | 'bundled' | 'mcp' / kind?: 'workflow' / immediate / isSensitive / userFacingName?`. | `types/command.ts:175-203` | Partial — v4 has name/description/whenToUse but missing the rest | **Yes (subset)** | Block J | Adopt: `whenToUse` (model-facing trigger description, distinct from `description`), `aliases`, `argumentHint`, `userInvocable`, `disableModelInvocation`, `isSensitive` (redacts args from history), `immediate` (bypass queue). Each is a real feature with real use cases. ~30 LOC. | RECOMMEND-ADD |
| 98 | `getCommandName(cmd)` / `isCommandEnabled(cmd)` — falls-back-to-name and falls-back-to-true helpers. | `types/command.ts:209-216` | No | Optional | Block J | Trivial. | OPTIONAL |
| 99 | `connectorText` types — `ConnectorTextBlock { type: 'connector_text', text }` + type guard. Stub for CONNECTOR_TEXT feature. | `types/connectorText.ts:1-9` | No | No (server-side feature) | N/A | The server-side CONNECTOR_TEXT summarization beta is firstParty-only. Bedrock doesn't return connector_text blocks. Skip. | DROP |
| 100 | Telemetry conventions: `TelemetrySafeError_I_VERIFIED_THIS_IS_NOT_CODE_OR_FILEPATHS` (long-name forces author confirmation) and `AnalyticsMetadata_I_VERIFIED_THIS_IS_NOT_CODE_OR_FILEPATHS`. | `utils/errors.ts:93-101` + various | No | Optional | All blocks (style guide) | The pattern: any time data is sent to telemetry, the type is intentionally awkward to use. Forces every author to confirm "yes, I checked this contains no PII". v5 has no telemetry surface today, but recording the pattern in the design doc has value. | OPTIONAL (style guide) |
| 101 | `RiskLevel = 'LOW' | 'MEDIUM' | 'HIGH'` + `PermissionExplanation { riskLevel, explanation, reasoning, risk }`. | `types/permissions.ts:401-410` | No | Optional | Block N | Useful framing for v5 if it ever has tool-risk classification. Keep one-line definition. ~5 LOC. | OPTIONAL |
| 102 | `AppState` fields exposing the SHAPE of "every dynamic thing in the agent": `settings, verbose, mainLoopModel, statusLineText, expandedView, isBriefOnly, kairosEnabled, toolPermissionContext, agentDefinitions, fileHistory, attribution, mcp{clients,tools,commands,resources}, plugins{enabled,disabled,commands,errors,installationStatus}, todos, notifications, sessionHooks, replContext{vmContext,registeredTools,console}, teamContext, inbox, workerSandboxPermissions, promptSuggestion, speculation, skillImprovement, authVersion, initialMessage, pendingPlanVerification, denialTracking, activeOverlays, fastMode, advisorModel, effortValue, isUltraplanMode`. | `state/AppStateStore.ts:89-452` | Partial — v4 has scattered globals | Optional | Block 0 | Use as **CHECKLIST** for v5 dataclass design: the relevant fields v5 needs to track are `verbose, settings, toolPermissionContext (mode + rules), todos, fileHistory, sessionHooks (skills), notifications (warnings to user), initialMessage`. Skip multi-agent / IDE / MCP fields. **Important checklist**, no LOC. | OPTIONAL (checklist) |
| 103 | `SerializedMessage` shape: `Message & { cwd, userType, entrypoint, sessionId, timestamp, version, gitBranch?, slug? }`. | `types/logs.ts:8-17` | Partial — v4 transcripts are JSON dicts of similar fields | **Yes** | Block H | Standardize the on-disk transcript record. Add `cwd`, `version`, `gitBranch` to v4's existing `{role, content, timestamp}`. ~10 LOC + a migration of old format if needed. | RECOMMEND-ADD |
| 104 | `LogOption` shape: 30 fields including `agentName, agentColor, agentSetting, isTeammate, leafUuid, summary, customTitle, tag, fileHistorySnapshots, attributionSnapshots, contextCollapseCommits, contextCollapseSnapshot, projectPath, prNumber, prUrl, prRepository, mode, worktreeSession, contentReplacements`. | `types/logs.ts:19-53` | Partial | Optional | Block H (session listing) | Most v5-relevant subset: `summary, customTitle, tag, projectPath, mode`. Each enables a real /resume UX feature (search by tag, filter by project, show summary in picker). ~20 LOC. | OPTIONAL |
| 105 | `FileHistorySnapshot` / `attributionSnapshots` — per-session file tracking for git-commit attribution and undo. | `types/logs.ts:188-219` | No | No (commit attribution out-of-scope, single-user) | N/A | Skip. | DROP |
| 106 | `ContextCollapseCommitEntry` (`marble-origami-commit`) + `ContextCollapseSnapshotEntry` (`marble-origami-snapshot`) — context-collapse persistence: archived messages stay in transcript; the COMMIT entry stores `{summaryUuid, summaryContent, summary, firstArchivedUuid, lastArchivedUuid}` to reconstruct the splice instruction. The discriminator name is deliberately obfuscated (`marble-origami-*`) to avoid leaking the gate name in external builds. | `types/logs.ts:255-295` | Partial — v4 compaction stores summary inline | Optional | Block A (compaction persistence) | High-craft pattern: persist enough to RECONSTRUCT the collapse, not the collapse itself. The archived messages are already in the transcript (as ordinary user/assistant turns); only the splice metadata needs new storage. Lets `/resume` re-derive the collapsed view without losing the originals. ~30 LOC if v5 wants resume-after-compaction. **High leverage**. | RECOMMEND-ADD |
| 107 | `ContentReplacementEntry` — content blocks whose in-context representation was replaced with a smaller stub (full content persisted elsewhere). Replayed on resume for prompt cache stability. Written once per enforcement pass. agentId scopes to subagent vs main thread. | `types/logs.ts:181-186` | No | Optional | Block A + Block T (large tool results) | Pairs with Runnable's `toolResultStorage.ts` (file persistence of >50KB tool results). Useful when v5 adds large-result persistence. | OPTIONAL |
| 108 | `WorktreeStateEntry` — last-wins record of "is this session inside a worktree". Restored on /resume only if the worktreePath still exists on disk. | `types/logs.ts:163-171` | No | No (worktree out-of-scope per single-user constraint) | N/A | Skip. | DROP |
| 109 | `Message.isMeta` flag — model-visible-but-UI-hidden. Used by system-generated prompts (proactive ticks, teammate messages, resource updates) routed through queue. | `types/textInputTypes.ts:330-336`, `state/AppStateStore.ts` references | Partial — v4 has system-meta separation but not consistent | **Yes** | Block A + Block H | One field on every message: `is_meta: bool` (default False). Hidden from chat.ipynb display but included in API request. Cleanly separates "what the user sees" from "what the model sees". ~5 LOC + display filter. | RECOMMEND-ADD |
| 110 | `QueuedCommand.priority: 'now' | 'next' | 'later'` — `now` interrupts in-flight tool, `next` drains between tool result and next API round-trip, `later` end-of-turn. | `types/textInputTypes.ts:280-294` | No | Optional | Block A (interruption) | If v5 ever supports user-typing-while-agent-runs in chat.ipynb, the priority semantics define the right interruption model. Otherwise skip. | OPTIONAL |

---

## ALREADY-IN-PLAN (verified covered by v5.0.1 plan / Q1 matrix)

| Item | Plan Block | Evidence |
|---|---|---|
| Compaction (cache-aware, autoCompact, summary persistence) | Block A | V5_PHASE_2_PLAN_v3.md:210-220 + Q1 row 28 |
| Token estimation | Block B | V5_PHASE_2_PLAN_v3.md:128 + Q1 row 16 |
| Memory extraction | Block H | V5_PHASE_2_PLAN_v3.md:289-297 |
| Error categorization + retry-after | Block L | V5_PHASE_2_PLAN_v3.md:299-333 |
| Parallel tool execution + dedup | Block N | V5_PHASE_2_PLAN_v3.md:335-348 |
| 7 v4 agent types + worktree | Block G | V5_PHASE_2_PLAN_v3.md:268-277 |
| 19 v4 slash commands | Block D | Q1 matrix rows 54-99 |
| 11 v4 tools | Block T | V5_PHASE_2_PLAN_v3.md:350-373 |
| Full UI rendering | Block E+F | V5_PHASE_2_PLAN_v3.md:222-248 |
| Cost tracking + limits + AGENT_STATUS | Block B+ | V5_PHASE_2_PLAN_v3.md:138-149 |

---

## OUT-OF-SCOPE-BY-CONSTRAINT (Runnable directories/files dropped categorically — utils slice)

| Category | Runnable items in this slice | Why dropped |
|---|---|---|
| OAuth/billing/auth (claude.ai) | `constants/oauth.ts`, `constants/system.ts:73-95` (attribution header), `utils/auth.ts`, `utils/billing.ts`, `utils/authPortable.ts`, `utils/authFileDescriptor.ts`, `utils/awsAuthStatusManager.ts`, `utils/aws.ts`, `migrations/migrate*` (all 11), `migrations/resetProToOpusDefault.ts`, `migrations/resetAutoModeOptInForDefaultOffer.ts`, `utils/managedEnv.ts`, `utils/extraUsage.ts` | Bedrock uses IAM role, single-user, no Anthropic-managed accounts |
| Multi-user / teammate / swarm | `state/teammateViewHelpers.ts`, `state/selectors.ts`, `utils/teammate.ts`, `utils/teammateMailbox.ts`, `utils/agentSwarmsEnabled.ts`, `utils/swarm/*`, `state/AppStateStore.ts` fields `teamContext / inbox / workerSandboxPermissions / pendingWorkerRequest / pendingSandboxRequest / showTeammateMessagePreview / viewSelectionMode / viewingAgentTaskId`, `utils/standaloneAgent.ts` | Single-user constraint #1 |
| IDE bridge / remote control / CCR | `utils/background/remote/*`, `state/AppState*` `replBridge*` fields (15+ fields), `bootstrap` `replBridge*`, `utils/desktopDeepLink.ts`, `utils/teleport.tsx`, `utils/deepLink/*`, `utils/dxt/*`, `utils/devtools.ts`, `utils/heapDumpService.ts`, `utils/heapDumpService.ts`, `utils/cron*` (cron tasks scheduling) | IDE/multi-process out-of-scope |
| Plugins / MCP / LSP | `schemas/hooks.ts` (plugin hooks via settings), `types/plugin.ts`, `utils/plugins/*`, `utils/mcp/*`, `services/mcp/*`, `utils/embeddedTools.ts` (bfs/ugrep) | Constraint #1+#9 |
| Ink/React UI | `utils/ink.ts`, `utils/staticRender.tsx`, `utils/exportRenderer.tsx`, `utils/asciicast.ts`, `utils/ansiToPng.ts`, `utils/ansiToSvg.ts`, `utils/imagePaste.ts`, `utils/imageResizer.ts`, `utils/markdown.ts`, `utils/cliHighlight.ts`, `utils/hyperlink.ts`, `utils/fpsTracker.ts`, `utils/fullscreen.ts`, `utils/theme.ts`, `state/AppState.tsx` (UI state), `utils/keyboardShortcuts.ts`, `utils/handlePromptSubmit.ts`, `state/AppStateStore.ts` `tungsten*/bagel*/computerUseMcpState`, `utils/logoV2Utils.ts`, `utils/status.tsx`, `utils/teleport.tsx`, `utils/statusNoticeDefinitions.tsx`, `utils/statusNoticeHelpers.ts`, `utils/dialogLaunchers.tsx` | Constraint #4 (chat.ipynb canonical UI) |
| Computer-use / Chrome / Voice | `utils/computerUse/*`, `utils/claudeInChrome/*`, `utils/voice/*`, `utils/processUserInput/*`, `state/AppStateStore.ts` `bagel*/tungsten*/computerUseMcpState/lastScreenshotDims/displayResolvedForApps`, `utils/modifiers.ts`, `utils/appleTerminalBackup.ts`, `utils/desktopDeepLink.ts`, `utils/deepLink/*`, `utils/claudeDesktop.ts` | Constraint #1 |
| Worktree / git / GitHub | `utils/worktree.ts`, `utils/getWorktreePathsPortable.ts`, `utils/git.ts`, `utils/gitDiff.ts`, `utils/gitSettings.ts`, `utils/github/*`, `constants/github-app.ts`, `utils/commitAttribution.ts`, `utils/attribution.ts`, `utils/attributionHooks.ts`, `utils/attributionTrailer.ts` | Worktree out-of-scope; single-user; no commit-attribution requirement (single author) |
| Settings layering / project config / managed/policy | `utils/settings/*`, `utils/cliArgs.ts`, `utils/managedEnv.ts`, `utils/markdownConfigLoader.ts`, `utils/policySettings.ts`, `utils/teamMemorySync.ts`, `utils/syncCacheState.ts`, `utils/agenticSessionSearch.ts`, `utils/sessionEnvVars.ts`, `migrations/*` | v5 has no enterprise policy or multi-user settings layers |
| Native installer / sandbox / sandbox / chrome native host | `utils/nativeInstaller/*`, `utils/sandbox/*`, `utils/secureStorage/*`, `utils/claudeInChrome/*`, `utils/binaryCheck.ts`, `utils/findExecutable.ts`, `utils/bundledMode.ts`, `utils/processUserInput/*`, `utils/preflightChecks.tsx`, `utils/doctorContextWarnings.ts`, `utils/doctorDiagnostic.ts`, `utils/autoUpdater.ts`, `utils/autoModeDenials.ts` | Cloud env, no installer/sandbox |
| Notebook (Jupyter) UI | `utils/notebooks.ts` (NotebookEdit) — keep TOOL but the editor utility is light-weight | The TOOL is in v4 already; specific util in `utils/`-subdirs not needed |
| Telemetry / billing-header / attestation | `utils/extraUsage.ts`, `utils/unaryLogging.ts`, `utils/sinks.ts`, `services/analytics/*`, `utils/userAgent.ts`, `utils/peerAddress.ts`, `utils/uds*` (UDS messaging), `utils/udsClient.ts`, `utils/udsMessaging.ts` | No telemetry surface |
| Async-mode classifier / auto / yolo | `utils/classifierApprovals.ts`, `utils/classifierApprovalsHook.ts`, `utils/autoModeDenials.ts`, `utils/denialTracking.ts`, `state/AppState*` `denialTracking` field, `types/permissions.ts:330-410` (yolo) | Out-of-scope (no UI for it; SageMaker single-user has no need for classifier auto-approve) |
| Speculation / proactive / kairos / brief / advisor / ultraplan / cron / fast-mode | `utils/speculation*`, `state/AppStateStore.ts` `speculation*/proactive*/kairos*/brief*/advisor*/ultraplan*/fastMode/effortValue/companion*/promptSuggestion`, `utils/proactive/*`, `utils/cron*`, `utils/effort.ts`, `utils/fastMode.ts`, `utils/advisor.ts`, `utils/sideQuery.ts` | Each is a feature gated on infra v5 doesn't have |
| Tmux / asciicast / VHS recording | `utils/tmuxSocket.ts`, `utils/asciicast.ts`, `utils/staticRender.tsx`, `utils/exportRenderer.tsx` | Notebook UI |
| Vim / editor key handling / typewriter / spinner-tree | `utils/vim/*`, `utils/typewriter*`, `utils/spinner*`, `state/*` `expandedView/spinnerTree`, `types/textInputTypes.ts` `VimTextInputProps/VimMode/InlineGhostText/PromptInputMode` | Notebook UI |

---

## Summary

- **Total Runnable utility files scanned**: ~280 top-level utils + 22 constants + 8 types (excl. generated) + 6 state + 11 migrations + 1 schema + 3 root files.
- **Already covered in v5.0.1 plan**: 10 broad categories (compaction, tokens, memory, errors, parallel tools, agents, commands, tools, rendering, cost — Q1 matrix has the 172-row breakdown).
- **NEW genuinely-useful patterns recommended for v5**: **24 RECOMMEND-ADD** items (rows #2 #10 #11 #12 #13 #15 #16 #18 #19 #20 #21 #24 #26 #28 #29 #30 #34 #35 #37 #39 #45 #50 #55 #63 #67 #71 #74 #76 #77 #78 #79 #80 #95 #97 #103 #106 #109 — note: counted distinct items above; #50 packages with #24).
- **Optional polish items**: ~25 OPTIONAL items (UX sweeteners, design-pattern checklists, future-proofing).
- **Drops**: ~35 items dropped categorically by constraint (multi-user, IDE bridge, plugins, ink/React UI, computer-use, OAuth, attestation, telemetry, attribution, worktree, settings layering, kairos/proactive/fast-mode, etc.).

### Highest-leverage NEW recommendations (must-add for v5.0.1)

These are the items where the cost is small and the bug class they fix is real:

1. **#37 system_prompt static/dynamic boundary + section-with-cacheBreak-flag pattern** — single biggest prompt-cache stability lever.
2. **#106 marble-origami-commit / -snapshot persistence pattern** — preserves originals while persisting only splice metadata; lets /resume reconstruct the collapsed view.
3. **#74 BEDROCK_EXTRA_PARAMS_HEADERS Set** — load-bearing on Bedrock; hardcodes the constraint that interleaved-thinking and context-1m must go through extraBodyParams.
4. **#11+#12+#13 errors module** — shortErrorStack, isFsInaccessible, ShellError class. Removes hundreds of bytes from tool_result errors and improves recovery semantics.
5. **#15+#16 token counting with iteration/usage anchor** — fixes parallel-tool-call undercount.
6. **#28+#29+#30 limits constants** — binary detection, image/PDF API limits, per-message tool result budget.
7. **#26 escapeXml/escapeXmlAttr** — prevents prompt corruption when memory/CLAUDE.md contains literal `<` or `&`.
8. **#19+#20 cancellation: combinedAbortSignal + AsyncLocalStorage cwd** — adds asyncio cancellation + per-task cwd that v4 lacks.
9. **#67 getSessionStartDate (memoized) + getLocalMonthYear** — cache-stability constants for date injection.
10. **#76+#77+#78+#79 prompt strings** — env block format, scratchpad section, DEFAULT_AGENT_PROMPT, "Notes" appendix. All battle-tested phrasings.
11. **#95+#97 PromptCommand `context: 'inline' | 'fork'` + `whenToUse` / `aliases` / `argumentHint` / `userInvocable` / `disableModelInvocation` / `isSensitive` / `immediate`** — real skill-system features v4 lacks.
12. **#34 Bedrock-relevant model cost table (Haiku 4.5 + Sonnet 4.6 only)** — concrete numbers, not the full Anthropic pricing matrix.

### Verdict

The Runnable types/constants/utils slice contributes a **prompt-cache-stability discipline + an error/abort/cancellation + a content-bounds + a transcript-shape** package that v4 only partially has. The above 24 recommendations are non-deferrable per v5.0.1's "no deferrals" rule because they are:
- (a) prerequisites for cache-stable subagent spawning + memory injection (#37 #67 #74),
- (b) bug-class fixes already encountered in v4 production (#11 #15 #26 #80),
- (c) constraints the Bedrock API enforces that v5 must check client-side (#28 #29 #30 #74),
- (d) shape decisions that, if deferred, force re-encoding the transcript later (#55 #103 #106 #109),
- (e) battle-tested prompt phrasings whose absence is a regression (#71 #76 #77 #78 #79).

Plan integration: route #37 #67 #74 #76 #77 #78 #79 #80 to **Block 0** (bootstrap/system-prompt); #11 #12 #13 #18 #19 to **Block L** (errors); #15 #16 #28 #29 #30 #34 to **Block B** (tokens/cost); #20 #21 to **Block G** (subagents); #24 #50 #95 #97 to **Block J** (skills/commands); #45 #2 to **Block T** (tools); #26 #55 #103 #106 #109 to **Block H/A** (storage/compaction). 

**No new Blocks required** — all 24 recommendations slot into existing v5.0.1 Blocks 0/A/B/G/H/J/L/N/T.
