# === DYNAMIC ===

This file is the explicit cache-boundary marker between the static prefix
(cached by Bedrock via `cache_control={"type":"ephemeral"}`) and the
per-turn dynamic content. The literal string `# === DYNAMIC ===` matches
the v4 marker (`compact_v4/MAIN/agent/sagemaker_agent.py:8177`) so the
Phase-1 BedrockClient cache splitter (`runtime/bedrock_client.py`)
continues to recognise it without modification.

Phase 6 ships this file as a structural marker. Phase 8+ runtime layers
(QueryEngine, skill auto-trigger, todo-restoration, file-restoration)
will inject content AFTER this boundary so the cached prefix stays
byte-stable across turns.

DO NOT edit this file unless you are intentionally moving the cache
boundary. Moving it invalidates the prompt cache for every active
session and triggers a CacheBreakWarning in `core.cache.detect_cache_break`.
