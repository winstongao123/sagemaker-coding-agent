# Sessions Directory

This directory stores conversation session files.

Each session is saved as a JSON file with:
- Session ID (timestamp-based)
- Title
- Message history
- Metadata

Sessions are automatically saved after each interaction.
