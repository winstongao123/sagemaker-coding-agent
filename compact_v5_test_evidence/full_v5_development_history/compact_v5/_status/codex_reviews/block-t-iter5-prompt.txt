You are reviewing Block T iter-5 of v5.0.1 — fixes for Codex iter-4
APPROVE_WITH_FIXES verdict (1 HIGH + 1 MEDIUM + 1 LOW).

Diff range: 36cad4f..HEAD (Block T iter-4 → Block T iter-5 commit 1f2211c).

iter-4 findings being closed:

1. HIGH — create_chart rejected schema-valid v4 calls without `filepath`.
   Fix at compact_v5/MAIN/agent/tools/v4_documents.py:
   `if not filepath: filepath = "chart.png"` before the .png extension
   normalization. v4 parity at sagemaker_agent.py:6049-6052; schema
   required=[data] now honored.
   Lock test: test_tool_create_chart_filepath_defaults_to_chart_png.

2. MEDIUM — create_chart combo did not support v4 shape.
   Fix at v4_documents.py: combo branch now FIRST checks for v4 keys
   `bar_values` + `line_values` and renders bar + line; uses ax.twinx()
   when `line_ylabel` is set so the line gets its own y-axis. v4 parity
   at sagemaker_agent.py:6194-6214. Series-based combo retained as
   fallback for the v5/Runnable shape.
   Lock test: test_tool_create_chart_v4_combo_shape (verifies bar_values
   + line_values + line_ylabel via twinx).

3. LOW — create_chart registration description listed only "bar | line |
   pie".
   Fix at v4_documents.py: description now lists all 8 v4 chart types
   ("bar, grouped_bar, stacked_bar, line, pie, scatter, horizontal_bar,
   combo") with the v4 WHEN-not-WHAT framing.

Tests: 770 pass + 14 skip (was 768 + 14; +2 net iter-4 lock tests).
verify_ship_zip.py: PASS (133 files / 376.0 KB / 36%).

Apply 3-axis review.

AXIS A — Errors / bugs / regressions
- Are the 3 iter-4 findings closed correctly without introducing new bugs?
- Does the default-filepath logic correctly honor the workspace gate
  (i.e. "chart.png" relative path resolves under CONFIG.workspace and
  doesn't bypass _validate_doc_path)?
- Does the v4-shape combo branch correctly handle the case when
  line_ylabel is omitted (single-axis combo)?
- Does ax.twinx() leak figure resources if an exception occurs
  mid-render? (figure is closed at the end of the try; the except
  fallback uses ax.bar — not ax2.)

AXIS B — v5 architecture + goals fit
- Constraint #1 (v4.10.10 baseline): all 8 v4 chart types now functional
  with v4 advertised data shapes (simple, x/y, series, combo). Are there
  any remaining v4 contract gaps for the 10 active Block T tools?
- Description text ports v4 wording verbatim. Acceptable or should we
  use the WHEN-not-WHAT v5 description style consistently?

AXIS C — Reference-repo coverage
- Spot-check v4 tool_create_chart for any other v4 behaviors v5 still
  misses that affect rendered output:
    - value labels on bars
    - tight_layout / bbox_inches on savefig
    - default dpi=300 (v5 default is matplotlib's default = 100)
    - size clamping
    - style reset between calls
  iter-3 acknowledged these as polish, NOT block-T-blocking. Confirm.

Goal: APPROVE so we can tag v5.0.1-block-t and move to Block J.

FINAL: APPROVE | APPROVE_WITH_FIXES | REJECT.

Be specific. Cite file:line for any finding. If APPROVE_WITH_FIXES, list
each fix with severity (CRITICAL / HIGH / MEDIUM / LOW). Polish items
that were explicitly deferred in iter-3 should not block APPROVE here.
