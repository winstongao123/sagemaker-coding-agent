Now I have the full picture. Producing the disposition review.

---

# R-tier Remaining Cleanup Disposition Review (independent Claude reviewer)

Repo: `D:\Github\sagemaker-coding-agent` · Date: 2026-05-06 · Branch: `v5-build`

## Per-row disposition

| Test | Decision | Required next action | Rationale |
|---|---|---|---|
| **R6** | AWS_REQUIRED | Single Haiku 4.5 AU `/dream` call on a 100-entry `memory.md` fixture with 5–10 explicit must-preserve markers, duplicate facts, and stale facts. Assert markers survive consolidation; check 4-phase prompt was used; record numeric cache fields. Cap `$0.30`, hard ceiling `$0.36`. | The local lock at `test_block_h_plus.py` only covers harness behaviour (4-phase prompt, lock, dry-run, rollback when consolidator raises). The R6 ready criterion is "required facts survive **real-LLM** consolidation," which depends on the model executing `DREAM_PROMPT_TEMPLATE` correctly on real input. No prior AWS row has exercised the consolidator with a 100-entry semantic checklist. |
| **R7** | AWS_REQUIRED | Two-turn session: turn 1 Haiku 4.5 AU, turn 2 Sonnet 4.5 AU on the same conversation. Assert post-switch Bedrock call uses Sonnet model id, conversation context survives, and cache fields are recorded (cold cache expected on first Sonnet turn). Cap `$0.50`. | R19-U10 Stage 8 explicitly used a **prebuilt `model_switch` fixture, not a live model switch**, and the resolution note in `OPTIMIZED_AWS_VALIDATION_PLAN.md` says R19-U10 "does not claim live model switching." R17 was Sonnet only (no switch). No AWS evidence currently shows a real Haiku→Sonnet switch with cache invariant. Local code paths (cache-break detection in `core/cache.py`, `_resolve_cache_block`) cannot prove server-side cache behaviour after a model id change. |
| **R9** | DISPOSITION_OK | Phase A `APPROVE_FOR_DISPOSITION` + disposition note + Phase C `DISPOSITION_OK` citing: `tests/integration/test_block_c_plus.py::test_approval_gate_blocks_until_approve` (approve+deny), `::test_approval_per_tool_always_allow` (sticky always-allow), `core/query_engine.py:1377-1457` (`require_tool_approval`/`_always_allowed`/`PermissionDialog` wiring), `tests/integration/test_block_b.py::test_audit_log_on_plan_mode_block` and `::test_audit_log_on_chat_response_contains_usage_and_thinking` (audit). Final wording: "approval flow validated by deterministic UI/dispatcher locks; AWS adds no new dimension because the approval gate runs **before** Bedrock is invoked." | The approve/deny/always paths and audit/diff hooks are entirely UI/dispatcher logic; the model is only required to emit a tool call. Local locks already exercise all three branches plus the always-allow sticky cache. A real Bedrock turn would only confirm "model can emit a tool call" — already covered by every other R-tier AWS row. |
| **R10** | DISPOSITION_OK | Phase A `APPROVE_FOR_DISPOSITION` + Phase C `DISPOSITION_OK` citing: `tests/integration/test_block_b_plus.py::test_session_save_load_preserves_cost`, `::test_save_resume_commands_restore_messages_and_cost`, `::test_resume_command_restores_agent_message_buffer`, `tests/integration/test_software_state.py::test_save_resume_round_trip_includes_todos_status_memory`. Plus: existing R-tier metrics ledger (`r_tier_metrics.jsonl`) already contains real Bedrock-paid rows whose cost/cache/tool data round-trips through SessionManager. Final wording: "PS#5/PS#6 cost+history persistence proven by deterministic SessionManager round-trip locks against the production `/save`/`/resume` command path; restart simulated by `TOKENS.reset()` between save and load." | The `$1.00` cap was sized to drive non-trivial cost into a save and reload it. The semantic claim ("cost and history survive a process restart") is purely a serialization property of `SessionManager` + `TokenTracker.restore`, both fully covered by existing locks. AWS spend would not increase the strength of the claim. |
| **R11** | AWS_REQUIRED | Shrunken R1-style fixture on Sonnet 4.5 AU: one chart task plus a bounded final assistant text, no Word document. Cap `$1.50` (matrix); expected actual `$0.50–$0.80`. Assert Sonnet model id is used, chart artifact integrity, no unrelated edits, quality `NEAR_IDEAL`/`WORKING_BUT_SUBOPTIMAL`. | R17 was deliberately a single-turn no-tool no-compaction Sonnet probe to isolate PS#4 thinking visibility; it is not a multi-turn workflow. R1 was Haiku. The user explicitly rejected deferments for the production-readiness package, and the production target is "single-person SageMaker software-building agent" which routinely runs Sonnet. There is no AWS evidence that v5's tool/cache/compaction loop survives Sonnet end-to-end. |
| **R12** | DISPOSITION_OK | Phase A `APPROVE_FOR_DISPOSITION` + Phase C `DISPOSITION_OK` citing: R8 mock for malformed JSON-string args (already READY), `tests/unit/test_errors.py` ErrorClassifier locks, `tests/integration/test_unicode_safe_output.py`, R1 call1→call2 history (real Unicode-stdout crash → fix → AWS pass preserved as diagnostic + `GENUINE_PASS`), and R17 evidence (`thinking_chars=1611`, `audit_thinking_chars=1611` on real Sonnet — non-trivial Unicode-capable text round-tripped through audit + telemetry). Final wording: "Unicode and malformed-args robustness validated by deterministic locks plus prior real-AWS evidence; no new AWS call adds a distinct production dimension." | The two claims (malformed args recovery, Unicode/RTL round-trip without crash) already have AWS evidence (R1) **plus** mock evidence (R8) **plus** local locks. The matrix cap of `$0.20` was sized to be cheap precisely because the surface is cheap to test deterministically. |
| **R18-E1** | DISPOSITION_OK | Phase A `APPROVE_FOR_DISPOSITION` + Phase C `DISPOSITION_OK` citing: `tests/unit/test_errors.py::test_throttle_classified` and `::test_service_unavailable_classified`, `tests/unit/test_retry.py` (jittered backoff schedule, MAX_RETRIES=4, `should_retry` matrix), and `runtime/bedrock_client.py:400-480` retry loop. Final wording: "throttle handling validated by deterministic classifier+policy locks; intentionally **not** forced on AWS because forcing 429 requires hammering Bedrock with unbounded retry spend, which is itself a `REJECT_PROCESS_UNSAFE` concern." | Real 429 cannot be reliably summoned without intentional rate violation, and the budget rule prohibits unbounded spend to reproduce a transient. The handling itself is deterministic Python code with explicit unit locks. |
| **R18-E3** | DISPOSITION_OK | Phase A `APPROVE_FOR_DISPOSITION` + Phase C `DISPOSITION_OK` citing: `tests/integration/test_block_b_plus.py::test_session_cost_limit_warns_at_100pct` (warn-and-continue contract per user 2026-05-03 Plan v3), `::test_cost_warning_resets_each_run`, `::test_cost_warning_fires_on_exact_100_percent`, `::test_cost_flush_on_exit_logs_final_cost`. Final wording: "cost-cap behaviour is intentionally warn-and-continue (user decision 2026-05-03), so 'halt timing near session cap' resolves to a deterministic warning lock, not a real-AWS halt event." | The matrix `ready_criteria` ("Cap warning/halt observed") was written before the warn-and-continue decision was made. The decision is now codebase-binding and locked. AWS spend would only re-prove the warning logs at 100%, which is already deterministic. |
| **R18-E4** | LOCAL_LOCK_REQUIRED | Add a local lock that defines what "skill alias activation" means in v5: either (a) an explicit alias-resolution surface in `skills/manager.py` with a deterministic test, or (b) an explicit "v5 ships activation by canonical skill name only; alias activation is not a v5.0.1 feature; the matrix row is reduced to a name-resolution lock" disposition. After lock lands, promote to DISPOSITION_OK. Do NOT spend AWS until the lock exists. | `skills/manager.py:469-475` only defines aliases for **cache-invalidation key resolution**, not for skill activation. There is no production "skill alias" surface in the v5 code. Spending AWS to test a feature that may not exist is the R4-style anti-pattern the user rejected. The semantic shape must be locked locally first. |
| **R18-E6** | DISPOSITION_OK | Phase A `APPROVE_FOR_DISPOSITION` + Phase C `DISPOSITION_OK` citing: `tests/tools/test_phase3_read_only_tools.py::test_read_file_missing_file_returns_error`, `tests/tools/test_phase4_mutating_tools.py::test_edit_file_missing_file_returns_error` and `::test_view_image_missing_file`, `::test_write_file_missing_args`, plus R19-U6 Stage 5 call2 AWS evidence (`malformed-output recovery, Phase C GENUINE_PASS`) for the recovery-on-tool-error model behaviour. | The "tool error surfaced to model" path is deterministic (`"not found"` strings). The "agent recovers" half is already proven on real AWS by R19-U6. No additional AWS spend adds a separate dimension. |
| **R18-E8** | DISPOSITION_OK | Phase A `APPROVE_FOR_DISPOSITION` + Phase C `DISPOSITION_OK` citing: `tests/integration/test_block_b_plus.py::test_filecache_thread_local_isolation` and `::test_filecache_save_and_restore_round_trip`, `tests/integration/test_software_subagent.py::test_spawn_subagent_result_envelope_has_tokens_files_and_heartbeat`, plus R19-U4 Stage 6 call2 AWS evidence (two real subagents, source-of-truth reconciliation, no failure-loop) and R19-U5 (three real subagents, expected missing-child handling). | Concurrency is a code/threading property; the deterministic lock proves no cross-thread state leak. Multi-subagent integrity at the model layer is already proven on real AWS by R19-U4/U5. |
| **R18-E10** | DISPOSITION_OK | Phase A `APPROVE_FOR_DISPOSITION` + Phase C `DISPOSITION_OK` citing: `tests/integration/test_block_b.py::test_audit_log_on_plan_mode_block` (mutating tool blocked + `plan_mode_blocked` audit event) and `core/query_engine.py:1226-1243` plan-mode allowlist enforcement. | Plan-mode allowlist is a deterministic dispatcher gate that runs before Bedrock is invoked. The audit lock already proves the block + audit row. No real-AWS dimension. |
| **R18-E11** | LOCAL_LOCK_REQUIRED | Extend `tests/integration/test_software_subagent.py` with a lock that combines `IterationBudget` exhaustion **with** a parent compaction-eligible state and asserts: timeout envelope is recorded, parent's `messages` survives compaction, parent can continue. After lock lands, promote to DISPOSITION_OK. | `test_budget_exhausted_task_tool_includes_recovery_envelope` covers timeout in isolation but does not cross-test it with compaction eligibility, which is the load-bearing R18-E11 claim. AWS would only re-prove a code-level interaction. |
| **R18-E13** | LOCAL_LOCK_REQUIRED | Add a Unicode/RTL round-trip lock through the memory write/read/dream path: write `memory.md` with mixed Unicode + RTL + emoji + surrogate-like sequences, run `run_dream` with a deterministic consolidator that echoes the input, assert byte-equivalence after read-back. After lock lands, promote to DISPOSITION_OK. | `test_unicode_safe_output.py` covers stdout, not the memory file path. The dream rollback lock does not exercise Unicode payloads. The semantic claim is a string-handling property that does not require Bedrock. |
| **R18-E14** | DISPOSITION_OK | Phase A `APPROVE_FOR_DISPOSITION` + Phase C `DISPOSITION_OK` citing: `tests/integration/test_block_h_plus.py::test_dream_rollback_on_failure` (mid-run exception → backup restored, original unchanged), `::test_dream_rollback_when_consolidator_returns_empty` (no write attempted on empty output), `::test_dream_creates_backup_before_write`, `runtime/dream.py:319-353` (backup-then-write + restore-on-OSError). | Atomicity/rollback is a deterministic file-system property fully covered by existing locks. The "interrupt mid-write" case is exactly the OSError write-failure path that the lock injects. AWS spend cannot strengthen this. |
| **R18-E15** | MAP_TO_EXISTING_AWS_OK | Phase C `DISPOSITION_OK` mapping to: R4 call1 AWS evidence (real Haiku, typed `compact_micro_start`/`compact_micro_end` with `trigger=cold_cache`, `microcompact_applied=true`, `microcompact_saved_tokens=50360`, two cleared old tool-result markers, numeric cache fields) plus `tests/unit/test_cache.py` cache-break detection locks. | R4 already validated A-16 cold-cache (i.e., effectively cache-TTL-expiry-style) behaviour mid-conversation on real Bedrock with the supported injectable threshold; the same code path covers TTL expiry without a 5m/1h wall-clock wait. The R18-E15 claim ("cache TTL behaviour during a conversation") is a strict subset of R4's evidence. |
| **R19-U8** | MAP_TO_EXISTING_AWS_OK | Phase C `DISPOSITION_OK` mapping to: R19-U10 Stage 8 call1 AWS evidence — real Haiku produced `final_coherence_report.md` with `latest runtime Python 3.12` and explicitly **not** `Python 3.10`; the resolution note in `OPTIMIZED_AWS_VALIDATION_PLAN.md` records "the stale Python 3.10 preference did not win." | R19-U8 ready criterion ("Final behavior uses Python 3.12 over older Python 3.10 preference") is byte-equivalent to a sub-claim already proven by R19-U10's transcript-substituted run on real Bedrock. No new spend would add a distinct production dimension. |
| **R19-U9** | AWS_REQUIRED | Bundle with R6 (same `/dream` runner): one Haiku 4.5 AU consolidation call against a fixture that satisfies **both** R6 (100 entries, duplicates, stale, must-preserve markers) **and** R19-U9 (semantic checklist of important facts to preserve). Per-test telemetry/metrics/quality/review-log written for both ids. Bundle cap `max($0.30, $0.30) = $0.30`, hard ceiling `$0.36`. | R6 and R19-U9 share an identical underlying production code path (`runtime/dream.py` + `DREAM_PROMPT_TEMPLATE`); the only delta is the assertion shape (general consolidation vs explicit semantic checklist). Bundling preserves the high-signal-non-overlap rule from `OPTIMIZED_AWS_VALIDATION_PLAN.md` because each row's evidence is a strict additional assertion on the same model output. |

## Gate change required to keep the default gate honest

Yes — `r_tier_gate.py` should grow a **third evidence kind** distinct from `READY` so that DISPOSITION_OK rows neither pass via faked AWS files nor fail the default gate.

Minimum required additions, narrowly scoped (mirrors the existing mock-only narrowing at `r_tier_gate.py:294, 311-316, 332-336`):

1. **New matrix attribute or status**: prefer adding `status: "DISPOSITION_OK"` to the matrix row (kind stays `real`) so the matrix totals and per-row caps remain unchanged. The gate keys disposition handling off `status`, not `kind`.
2. **New Phase A token**: `APPROVE_FOR_DISPOSITION` accepted at `_phase_a_latest_decision_is_approval`. Mock `APPROVE_FOR_LOCAL_MOCK` is **not** a substitute (different evidence shape).
3. **New required file**: `_status/codex_reviews/r-tier-<TEST>-disposition-iter<N>.md` that names the mapped existing locks/AWS evidence and explains why no new AWS spend is needed. Required only when `status=DISPOSITION_OK`.
4. **Phase C verdict allowlist**: accept `DISPOSITION_OK` (in addition to `GENUINE_PASS`) for disposition rows. Real-AWS rows must still produce `GENUINE_PASS`; mock rows still produce `GENUINE_PASS`.
5. **Metrics row**: zero-cost row with `cost_usd=0.0`, `verdict=DISPOSITION_OK`. Add `DISPOSITION_OK` to `{GENUINE_PASS, READY}` allowlist for completed pass. Cost ceiling already handles zero correctly.
6. **Review-log token**: `DISPOSITION_OK` accepted alongside `READY` and `ESCALATED` for disposition rows.
7. **No `local-call`/`aws-call` raw log required** for disposition rows; the disposition note replaces the run log. Telemetry and quality files are **not** required (no run happened) — but the disposition note must cite the telemetry/quality files of the mapped prior evidence by path.
8. **Final summary**: when run without `--test`, the gate must print a per-row tag (`READY`, `DISPOSITION_OK`, `ESCALATED`, `MOCK_READY`) so `R-tier gate PASSED` is never ambiguous about what kind of evidence each row carries.

Without this change, every DISPOSITION_OK row above would either (a) fail the default gate because no AWS call log / telemetry / metrics-pass row exists, or (b) be forced through by writing fake files — exactly the failure mode the user is asking the reviewer to block.

## Summary of next-action plan

- **5 LOCAL_LOCK_REQUIRED before disposition can ship**: R18-E4 (or convert to explicit non-feature disposition), R18-E11, R18-E13. (`R18-E4` is the only one with a substantive design question — no alias activation surface exists today.)
- **3 AWS_REQUIRED**: R6, R7, R11. Total expected new AWS spend at hard ceilings: `$0.36 + $0.60 + $1.80 = $2.76`. With R19-U9 bundled into R6, no additional spend.
- **9 DISPOSITION_OK** (R9, R10, R12, R18-E1, R18-E3, R18-E6, R18-E8, R18-E10, R18-E14): requires the gate change above plus per-row disposition notes. Zero new AWS spend.
- **3 MAP_TO_EXISTING_AWS_OK** (R18-E15→R4, R19-U8→R19-U10, R19-U9→R6 bundle): requires the gate change above plus mapped-evidence citations. Zero new AWS spend (R19-U9 budget covered by R6 call).
- **No REJECT_PROCESS_UNSAFE rows** identified, provided the gate change lands before any DISPOSITION_OK row is marked READY.

APPROVE_DISPOSITION_PLAN
